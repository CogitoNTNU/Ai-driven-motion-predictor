{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"AI-Driven Motion Predictor","text":"<p>A full-stack application for AI-powered stock analysis featuring a React frontend chat interface and a FastAPI backend with LangGraph agents.</p>   ![GitHub Workflow Status (with event)](https://img.shields.io/github/actions/workflow/status/CogitoNTNU/AI-driven-motion-predictor/ci.yml) ![GitHub top language](https://img.shields.io/github/languages/top/CogitoNTNU/AI-driven-motion-predictor) ![GitHub language count](https://img.shields.io/github/languages/count/CogitoNTNU/AI-driven-motion-predictor) [![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT) [![Project Version](https://img.shields.io/badge/version-0.0.1-blue)](https://img.shields.io/badge/version-0.0.1-blue)   \ud83d\udccb Table of contents   - [AI-Driven Motion Predictor](#ai-driven-motion-predictor)   - [Project Structure](#project-structure)   - [Description](#description)   - [Prerequisites](#prerequisites)   - [Quick Start](#quick-start)   - [Frontend](#frontend)   - [Backend](#backend)   - [Environment Variables](#environment-variables)   - [Documentation](#documentation)   - [Testing](#testing)   - [Team](#team)   - [License](#license)"},{"location":"#project-structure","title":"Project Structure","text":"<pre><code>AI-driven-motion-predictor/\n\u251c\u2500\u2500 src/\n\u2502   \u251c\u2500\u2500 app/                 # React frontend (see src/app/README.md)\n\u2502   \u2502   \u251c\u2500\u2500 package.json     # Node.js dependencies: React 19, Vite, Tailwind CSS\n\u2502   \u2502   \u2514\u2500\u2500 ...\n\u2502   \u251c\u2500\u2500 api/                 # FastAPI backend (see src/api/README.md)\n\u2502   \u2502   \u251c\u2500\u2500 pyproject.toml   # Python dependencies: FastAPI, LangGraph, LangChain\n\u2502   \u2502   \u251c\u2500\u2500 main.py          # FastAPI application entry point\n\u2502   \u2502   \u2514\u2500\u2500 ...\n\u2502   \u2514\u2500\u2500 Kaare/               # Core ML/stock analysis library\n\u251c\u2500\u2500 pyproject.toml           # Root Python project configuration\n\u251c\u2500\u2500 requirements.txt         # Legacy pip requirements\n\u251c\u2500\u2500 .env.example             # Environment variable template\n\u2514\u2500\u2500 README.md                # This file\n</code></pre>"},{"location":"#description","title":"Description","text":"<p>This project provides an AI-powered stock analysis platform with:</p> <ul> <li>Frontend: React 19 + TypeScript + Vite chat interface using shadcn/ui components and Vercel AI SDK for streaming   responses</li> <li>Backend: FastAPI server with LangGraph multi-agent architecture (supervisor pattern) for stock market analysis</li> <li>Core ML: PyTorch, Transformers, and financial data integrations (yfinance, Finnhub)</li> </ul>"},{"location":"#prerequisites","title":"Prerequisites","text":""},{"location":"#required","title":"Required","text":"<ul> <li>Git: Version control Download Git</li> <li>Python: 3.12 or 3.13 Download Python</li> <li>uv: Modern Python package manager Install UV <code>sh   curl -LsSf https://astral.sh/uv/install.sh | sh</code></li> <li>Node.js: 20 or higher Download Node.js</li> <li>pnpm: Package manager for Node.js   <code>sh   npm install -g pnpm   # or   curl -fsSL https://get.pnpm.io/install.sh | sh -</code></li> </ul>"},{"location":"#optional","title":"Optional","text":"<ul> <li>Docker: For containerized development Download Docker</li> <li>PostgreSQL/ParadeDB: For persistent data storage (can use Docker)</li> </ul>"},{"location":"#quick-start","title":"Quick Start","text":""},{"location":"#1-clone-the-repository","title":"1. Clone the Repository","text":"<pre><code>git clone https://github.com/CogitoNTNU/AI-driven-motion-predictor.git\ncd AI-driven-motion-predictor\n</code></pre>"},{"location":"#2-configure-environment-variables","title":"2. Configure Environment Variables","text":"<p>Copy the example environment file:</p> <pre><code>cp .env.example .env\n</code></pre> <p>Edit <code>.env</code> with your API keys:</p> <pre><code># Required for LLM functionality\nOPENAI_API_KEY=sk-your-key-here\nOPENAI_BASE_URL=https://api.openai.com/v1\n\n# Required for stock data\nFINNHUB_API_KEY=your-finnhub-key\nALPHAVANTAGE_API_KEY=your-alphavantage-key\n\n# Required for ML models\nHF_TOKEN=hf-your-huggingface-token\n\n# Optional: Database (uses defaults if not set)\nDB_USER=admin\nDB_PASSWORD=changeme\nDB_NAME=motion_predictor\n</code></pre>"},{"location":"#3-install-dependencies","title":"3. Install Dependencies","text":"<p>Root Python project (for core ML library):</p> <pre><code>uv sync\n</code></pre> <p>Backend API:</p> <pre><code>cd src/api\nuv sync\ncd ../..\n</code></pre> <p>Frontend:</p> <pre><code>cd src/app\npnpm install\ncd ../..\n</code></pre>"},{"location":"#4-start-development-servers","title":"4. Start Development Servers","text":"<p>Terminal 1 - Backend:</p> <pre><code>cd src/api\nuv run uvicorn main:app --reload --host 0.0.0.0 --port 8000\n</code></pre> <p>Terminal 2 - Frontend:</p> <pre><code>cd src/app\npnpm dev\n</code></pre> <p>The application will be available at:</p> <ul> <li>Frontend: http://localhost:5173</li> <li>Backend API: http://localhost:8000</li> <li>API Docs: http://localhost:8000/docs (Swagger UI)</li> </ul>"},{"location":"#frontend","title":"Frontend","text":"<p>The frontend is a React 19 + TypeScript application built with Vite and styled with Tailwind CSS 4 and shadcn/ui components.</p> <p>Key technologies (from <code>src/app/package.json</code>):</p> <ul> <li>React 19.2.4 with TypeScript 5.9.3</li> <li>Vite 7.3.1 build tool</li> <li>Tailwind CSS 4.2.1</li> <li>Vercel AI SDK React 3.0.118 for streaming chat</li> <li>Recharts 3.8.0 for data visualization</li> <li>shadcn/ui component library with Radix UI primitives</li> </ul> <p>Quick commands:</p> <pre><code>cd src/app\n\n# Install dependencies\npnpm install\n\n# Start development server\npnpm dev\n\n# Build for production\npnpm build\n\n# Format code\npnpm format\n\n# Type checking\npnpm typecheck\n</code></pre> <p>Detailed documentation: See src/app/README.md</p>"},{"location":"#backend","title":"Backend","text":"<p>The backend is a FastAPI server providing stock analysis through LangGraph agents with streaming responses.</p> <p>Key technologies (from <code>src/api/pyproject.toml</code>):</p> <ul> <li>FastAPI 0.135.1 with Uvicorn 0.30.0</li> <li>Python 3.12 or 3.13</li> <li>LangGraph 0.2.0+ for agent workflows</li> <li>LangChain 0.3.0+ and LangChain-OpenAI 0.2.0+</li> <li>LangGraph-Supervisor 0.0.15+ for multi-agent orchestration</li> <li>yfinance 0.2.0+ for financial data</li> </ul> <p>Quick commands:</p> <pre><code>cd src/api\n\n# Install dependencies\nuv sync\n\n# Run development server with hot reload\nuv run uvicorn main:app --reload --host 0.0.0.0 --port 8000\n\n# Or run directly\nuv run main.py\n</code></pre> <p>API Endpoints:</p> <ul> <li><code>GET /</code> - API info</li> <li><code>GET /health</code> - Health check</li> <li><code>POST /api/chat</code> - Streaming chat endpoint (AI SDK format)</li> <li><code>POST /api/chat/sync</code> - Synchronous chat endpoint</li> </ul> <p>Detailed documentation: See src/api/README.md</p>"},{"location":"#environment-variables","title":"Environment Variables","text":"<p>The application uses environment variables from <code>.env</code> in the project root. Key variables:</p> Variable Required Description <code>OPENAI_API_KEY</code> Yes OpenAI API key for LLM <code>OPENAI_BASE_URL</code> Yes OpenAI API base URL <code>MODEL_NAME</code> No Model to use (default: gpt-4o-mini) <code>FINNHUB_API_KEY</code> Yes* Finnhub API for stock data <code>ALPHAVANTAGE_API_KEY</code> Yes* Alpha Vantage API for stock data <code>HF_TOKEN</code> Yes HuggingFace token for FinBERT models <code>DB_USER</code> No PostgreSQL username <code>DB_PASSWORD</code> No PostgreSQL password <code>DB_NAME</code> No PostgreSQL database name <code>DB_PORT</code> No PostgreSQL port (default: 5432) <code>VITE_API_URL</code> No Frontend API URL (default: http://localhost:8000) <p>* At least one stock data provider required</p> <p>See <code>.env.example</code> for the complete list.</p>"},{"location":"#documentation","title":"Documentation","text":"<p>To build and preview the documentation site locally:</p> <pre><code># Install docs dependencies (from root pyproject.toml docs group)\nuv sync --group docs\n\n# Build and serve\nuv run mkdocs build\nuv run mkdocs serve\n</code></pre> <p>The documentation will be available at http://127.0.0.1:8000/.</p>"},{"location":"#testing","title":"Testing","text":"<p>Run the test suite from the project root:</p> <pre><code>uv run pytest --doctest-modules --cov=src --cov-report=html\n</code></pre> <p>View the coverage report at <code>htmlcov/index.html</code>.</p>"},{"location":"#set-up-pre-commit-hooks-development","title":"Set up pre-commit hooks (development):","text":"<pre><code>uv run pre-commit install\n</code></pre>"},{"location":"#team","title":"Team","text":"<p>This project would not have been possible without the hard work and dedication of all of the contributors. Thank you for the time and effort you have put into making this project a reality.</p> <p></p>"},{"location":"#license","title":"License","text":"<p>Distributed under the MIT License. See <code>LICENSE</code> for more information.</p>"},{"location":"reference/src/Kaare/","title":"Kaare","text":""},{"location":"reference/src/Kaare/#src.Kaare","title":"src.Kaare","text":"<p>Kaare \u2014 async cache-first data layer for stock and macro data.</p> <p>Example::</p> <pre><code>import asyncio\nimport datetime\nfrom Kaare import KaareClient\n\nasync def main():\n    async with KaareClient() as client:\n        stocks = await client.get_stock_ohlcv(\n            \"AAPL\",\n            datetime.date(2024, 1, 1),\n            datetime.date(2024, 3, 1),\n        )\n        macro = await client.get_macro_data(\n            datetime.date(2024, 1, 1),\n            datetime.date(2024, 3, 1),\n        )\n\nasyncio.run(main())\n</code></pre> <p>Modules:</p> <ul> <li> <code>client</code>           \u2013            <p>KaareClient \u2014 the public fa\u00e7ade for the Kaare data layer.</p> </li> <li> <code>config</code>           \u2013            <p>Settings loaded from environment / .env file.</p> </li> <li> <code>db</code>           \u2013            <p>Database sub-package (connection pool, migrations, repository).</p> </li> <li> <code>debug_hf</code>           \u2013            <p>Quick debug: print extra_fields for the first few rows of a subset.</p> </li> <li> <code>models</code>           \u2013            <p>Domain models for the Kaare data layer.</p> </li> <li> <code>pipeline</code>           \u2013            <p>FinBERT sentiment pipeline \u2014 download, score, aggregate.</p> </li> <li> <code>providers</code>           \u2013            <p>Data provider sub-package (yfinance).</p> </li> <li> <code>seed</code>           \u2013            <p>Seed script \u2014 populate Kaare with the 10 largest stocks, macro data, and FinBERT sentiment.</p> </li> <li> <code>sentiment</code>           \u2013            <p>FinBERT-based sentiment analysis for market news.</p> </li> <li> <code>smoke_test</code>           \u2013            <p>Quick smoke test for KaareClient against the remote DB.</p> </li> <li> <code>smoke_test_sentiment</code>           \u2013            <p>Smoke tests for get_stock_news_sentiment routing logic.</p> </li> </ul>"},{"location":"reference/src/Kaare/client/","title":"client","text":""},{"location":"reference/src/Kaare/client/#src.Kaare.client","title":"src.Kaare.client","text":"<p>KaareClient \u2014 the public fa\u00e7ade for the Kaare data layer.</p> <p>Classes:</p> <ul> <li> <code>KaareClient</code>           \u2013            <p>Cache-first async data client for stock and macro-economic data.</p> </li> </ul>"},{"location":"reference/src/Kaare/client/#src.Kaare.client.KaareClient","title":"KaareClient","text":"<pre><code>KaareClient(pool: Pool | None = None)\n</code></pre> <p>Cache-first async data client for stock and macro-economic data.</p> <p>Usage::</p> <pre><code>async with KaareClient() as client:\n    stocks = await client.get_stock_ohlcv(\"AAPL\", start, end)\n    macro  = await client.get_macro_data(start, end)\n</code></pre> <p>On every request the database is queried first. Missing dates are fetched from yfinance, then stored in the database before returning.</p> <p>Methods:</p> <ul> <li> <code>close</code>             \u2013              <p>Close the connection pool (only if we own it).</p> </li> <li> <code>get_macro_data</code>             \u2013              <p>Return macro data (gold, Treasury 10Y, sentiment) in [start, end].</p> </li> <li> <code>get_stock_news_sentiment</code>             \u2013              <p>Return FinBERT sentiment for symbol over [start, end].</p> </li> <li> <code>get_stock_ohlcv</code>             \u2013              <p>Return OHLCV data for symbol in [start, end], fetching from upstream if needed.</p> </li> <li> <code>initialize</code>             \u2013              <p>Create the connection pool and run schema migrations.</p> </li> </ul> Source code in <code>src/Kaare/client.py</code> <pre><code>def __init__(self, pool: asyncpg.Pool | None = None) -&gt; None:\n    self._yf = YFinanceProvider()\n    self._finnhub = FinnhubProvider()\n    self._analyzer = FinBERTAnalyzer()\n    self._pool: asyncpg.Pool | None = pool\n    self._owns_pool: bool = pool is None  # only close pools we created\n</code></pre>"},{"location":"reference/src/Kaare/client/#src.Kaare.client.KaareClient.close","title":"close  <code>async</code>","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Close the connection pool (only if we own it).</p> Source code in <code>src/Kaare/client.py</code> <pre><code>async def close(self) -&gt; None:\n    \"\"\"Close the connection pool (only if we own it).\"\"\"\n    if self._pool and self._owns_pool:\n        await self._pool.close()\n</code></pre>"},{"location":"reference/src/Kaare/client/#src.Kaare.client.KaareClient.get_macro_data","title":"get_macro_data  <code>async</code>","text":"<pre><code>get_macro_data(start: date, end: date, symbols: list[str] | None = None) -&gt; list[MacroData]\n</code></pre> <p>Return macro data (gold, Treasury 10Y, sentiment) in [start, end].</p> <p>Gold and Treasury yields are fetched from yfinance for missing dates. Sentiment is sourced from the <code>daily_market_sentiment</code> table, which is populated by running the FinBERT pipeline (see :mod:<code>Kaare.pipeline</code>).</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>list[MacroData]</code>           \u2013            <p>List of :class:<code>~Kaare.models.MacroData</code> ordered by date ascending.</p> </li> </ul> Source code in <code>src/Kaare/client.py</code> <pre><code>async def get_macro_data(\n    self,\n    start: datetime.date,\n    end: datetime.date,\n    symbols: list[str] | None = None,\n) -&gt; list[MacroData]:\n    \"\"\"Return macro data (gold, Treasury 10Y, sentiment) in [start, end].\n\n    Gold and Treasury yields are fetched from yfinance for missing dates.\n    Sentiment is sourced from the ``daily_market_sentiment`` table, which is\n    populated by running the FinBERT pipeline (see :mod:`Kaare.pipeline`).\n\n    Args:\n        start: Inclusive start date.\n        end: Inclusive end date.\n        symbols: Unused \u2014 kept for API compatibility. Sentiment now comes\n            from the pre-computed ``daily_market_sentiment`` table.\n\n    Returns:\n        List of :class:`~Kaare.models.MacroData` ordered by date ascending.\n    \"\"\"\n    cached = await repository.get_macro_data(self._db, start, end)\n    cached_dates = {r.date for r in cached}\n\n    expected = _business_dates(start, end)\n    missing = expected - cached_dates\n\n    if missing:\n        miss_start = min(missing)\n        miss_end = max(missing)\n\n        gold: dict[datetime.date, float] = {}\n        try:\n            gold = await self._yf.get_gold_prices(miss_start, miss_end)\n        except Exception as exc:\n            logger.error(\"yfinance gold failed: %s\", _safe_str(exc))\n\n        yields: dict[datetime.date, float] = {}\n        try:\n            yields = await self._yf.get_treasury_yields(miss_start, miss_end)\n        except Exception as exc:\n            logger.error(\"yfinance Treasury failed: %s\", _safe_str(exc))\n\n        vix: dict[datetime.date, float] = {}\n        try:\n            vix = await self._yf.get_vix(miss_start, miss_end)\n        except Exception as exc:\n            logger.error(\"yfinance VIX failed: %s\", _safe_str(exc))\n\n        all_dates = set(gold) | set(yields) | set(vix) | missing\n        new_records = [\n            MacroData(\n                date=d,\n                gold_price=gold.get(d),\n                treasury_yield_10y=yields.get(d),\n                vix=vix.get(d),\n            )\n            for d in sorted(all_dates)\n            if start &lt;= d &lt;= end\n        ]\n        if new_records:\n            await repository.upsert_macro_data(self._db, new_records)\n\n    # Pull pre-computed sentiment from daily_market_sentiment into macro_data\n    await repository.sync_sentiment_to_macro(self._db, start, end)\n\n    return await repository.get_macro_data(self._db, start, end)\n</code></pre>"},{"location":"reference/src/Kaare/client/#src.Kaare.client.KaareClient.get_macro_data(start)","title":"<code>start</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive start date.</p>"},{"location":"reference/src/Kaare/client/#src.Kaare.client.KaareClient.get_macro_data(end)","title":"<code>end</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive end date.</p>"},{"location":"reference/src/Kaare/client/#src.Kaare.client.KaareClient.get_macro_data(symbols)","title":"<code>symbols</code>","text":"(<code>list[str] | None</code>, default:                   <code>None</code> )           \u2013            <p>Unused \u2014 kept for API compatibility. Sentiment now comes from the pre-computed <code>daily_market_sentiment</code> table.</p>"},{"location":"reference/src/Kaare/client/#src.Kaare.client.KaareClient.get_stock_news_sentiment","title":"get_stock_news_sentiment  <code>async</code>","text":"<pre><code>get_stock_news_sentiment(symbol: str, start: date | None = None, end: date | None = None) -&gt; NewsSentimentResult\n</code></pre> <p>Return FinBERT sentiment for symbol over [start, end].</p> <p>If the date range ends more than one year ago, data is read directly from the pre-seeded <code>daily_ticker_sentiment</code> table (fast, no API call). For recent data (within the last year) the Finnhub API is called, articles are scored with FinBERT, and results are persisted before returning.</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>A</code> (              <code>NewsSentimentResult</code> )          \u2013            <p>class:<code>~Kaare.models.NewsSentimentResult</code> with the average</p> </li> <li> <code>NewsSentimentResult</code>           \u2013            <p>sentiment score in <code>[-1.0, 1.0]</code> and a per-date breakdown.</p> </li> </ul> Source code in <code>src/Kaare/client.py</code> <pre><code>async def get_stock_news_sentiment(\n    self,\n    symbol: str,\n    start: datetime.date | None = None,\n    end: datetime.date | None = None,\n) -&gt; NewsSentimentResult:\n    \"\"\"Return FinBERT sentiment for *symbol* over [start, end].\n\n    If the date range ends more than one year ago, data is read directly from\n    the pre-seeded ``daily_ticker_sentiment`` table (fast, no API call).\n    For recent data (within the last year) the Finnhub API is called, articles\n    are scored with FinBERT, and results are persisted before returning.\n\n    Args:\n        symbol: Stock ticker (e.g. ``\"AAPL\"``).\n        start: Inclusive start date. Defaults to 7 days before *end*.\n        end: Inclusive end date. Defaults to today.\n\n    Returns:\n        A :class:`~Kaare.models.NewsSentimentResult` with the average\n        sentiment score in ``[-1.0, 1.0]`` and a per-date breakdown.\n    \"\"\"\n    today = datetime.date.today()\n    end = end or today\n    start = start or (end - datetime.timedelta(days=7))\n\n    one_year_ago = today - datetime.timedelta(days=365)\n\n    if end &lt;= one_year_ago:\n        # Historical \u2014 read from pre-seeded daily_ticker_sentiment table\n        rows = await repository.get_daily_ticker_sentiment(\n            self._db, symbol, start, end\n        )\n        if not rows:\n            logger.warning(\n                \"No seeded sentiment for %s between %s and %s.\", symbol, start, end\n            )\n            return NewsSentimentResult(\n                symbol=symbol, start=start, end=end, article_count=0, avg_score=0.0\n            )\n\n        daily_scores = {date: score for date, score, _ in rows}\n        total_articles = sum(count for _, _, count in rows)\n        avg_score = sum(score for _, score, _ in rows) / len(rows)\n\n        return NewsSentimentResult(\n            symbol=symbol,\n            start=start,\n            end=end,\n            article_count=total_articles,\n            avg_score=avg_score,\n            daily_scores=daily_scores,\n        )\n\n    # Recent \u2014 check DB freshness before hitting Finnhub + FinBERT.\n    # If this ticker's sentiment was aggregated within the last 6 hours,\n    # return the cached DB rows directly without calling the external API.\n    _SIX_HOURS = datetime.timedelta(hours=6)\n    last_updated = await repository.get_ticker_sentiment_last_updated(self._db, symbol)\n    if last_updated is not None:\n        now_utc = datetime.datetime.now(datetime.timezone.utc)\n        if now_utc - last_updated &lt; _SIX_HOURS:\n            logger.debug(\n                \"Sentiment for %s is fresh (updated %s). Skipping Finnhub+FinBERT.\",\n                symbol, last_updated,\n            )\n            db_rows = await repository.get_daily_ticker_sentiment(self._db, symbol, start, end)\n            daily_scores = {date: score for date, score, _ in db_rows}\n            total_articles = sum(count for _, _, count in db_rows)\n            avg_score = sum(score for _, score, _ in db_rows) / len(db_rows) if db_rows else 0.0\n            return NewsSentimentResult(\n                symbol=symbol,\n                start=start,\n                end=end,\n                article_count=total_articles,\n                avg_score=avg_score,\n                daily_scores=daily_scores,\n            )\n\n    articles = await self._finnhub.fetch_raw_news(symbol, start, end)\n\n    if not articles:\n        logger.warning(\n            \"No news found for %s between %s and %s.\", symbol, start, end\n        )\n        return NewsSentimentResult(\n            symbol=symbol, start=start, end=end, article_count=0, avg_score=0.0\n        )\n\n    ids = await repository.insert_raw_news_returning_ids(self._db, articles)\n\n    # Fetch already-scored articles to avoid re-running FinBERT\n    existing_scores = await repository.get_article_sentiment_by_ids(self._db, ids)\n\n    unscored_pairs = [\n        (i, a) for i, a in zip(ids, articles) if i not in existing_scores\n    ]\n\n    if unscored_pairs:\n        unscored_ids, unscored_articles = zip(*unscored_pairs)\n        new_scores = await self._analyzer.score_articles(\n            [a.text for a in unscored_articles]\n        )\n        await repository.insert_article_sentiment_batch(\n            self._db, list(zip(unscored_ids, new_scores))\n        )\n        for i, score in zip(unscored_ids, new_scores):\n            existing_scores[i] = score\n\n    await repository.aggregate_daily_ticker_sentiment(self._db)\n\n    # Read back from daily_ticker_sentiment so the return value is always\n    # consistent with the DB (which aggregates ALL articles, not just this batch)\n    db_rows = await repository.get_daily_ticker_sentiment(\n        self._db, symbol, start, end\n    )\n    daily_scores = {date: score for date, score, _ in db_rows}\n    total_articles = sum(count for _, _, count in db_rows)\n    avg_score = (\n        sum(score for _, score, _ in db_rows) / len(db_rows) if db_rows else 0.0\n    )\n\n    return NewsSentimentResult(\n        symbol=symbol,\n        start=start,\n        end=end,\n        article_count=total_articles,\n        avg_score=avg_score,\n        daily_scores=daily_scores,\n    )\n</code></pre>"},{"location":"reference/src/Kaare/client/#src.Kaare.client.KaareClient.get_stock_news_sentiment(symbol)","title":"<code>symbol</code>","text":"(<code>str</code>)           \u2013            <p>Stock ticker (e.g. <code>\"AAPL\"</code>).</p>"},{"location":"reference/src/Kaare/client/#src.Kaare.client.KaareClient.get_stock_news_sentiment(start)","title":"<code>start</code>","text":"(<code>date | None</code>, default:                   <code>None</code> )           \u2013            <p>Inclusive start date. Defaults to 7 days before end.</p>"},{"location":"reference/src/Kaare/client/#src.Kaare.client.KaareClient.get_stock_news_sentiment(end)","title":"<code>end</code>","text":"(<code>date | None</code>, default:                   <code>None</code> )           \u2013            <p>Inclusive end date. Defaults to today.</p>"},{"location":"reference/src/Kaare/client/#src.Kaare.client.KaareClient.get_stock_ohlcv","title":"get_stock_ohlcv  <code>async</code>","text":"<pre><code>get_stock_ohlcv(symbol: str, start: date, end: date) -&gt; list[StockOHLCV]\n</code></pre> <p>Return OHLCV data for symbol in [start, end], fetching from upstream if needed.</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>list[StockOHLCV]</code>           \u2013            <p>List of :class:<code>~Kaare.models.StockOHLCV</code> ordered by date ascending.</p> </li> </ul> Source code in <code>src/Kaare/client.py</code> <pre><code>async def get_stock_ohlcv(\n    self,\n    symbol: str,\n    start: datetime.date,\n    end: datetime.date,\n) -&gt; list[StockOHLCV]:\n    \"\"\"Return OHLCV data for *symbol* in [start, end], fetching from upstream if needed.\n\n    Args:\n        symbol: Ticker symbol (e.g. ``\"AAPL\"``).\n        start: Inclusive start date.\n        end: Inclusive end date.\n\n    Returns:\n        List of :class:`~Kaare.models.StockOHLCV` ordered by date ascending.\n    \"\"\"\n    cached = await repository.get_stock_ohlcv(self._db, symbol, start, end)\n    cached_dates = {r.date for r in cached}\n\n    expected = _business_dates(start, end)\n    missing = expected - cached_dates\n\n    if missing:\n        miss_start = min(missing)\n        miss_end = max(missing)\n        fetched: list[StockOHLCV] = []\n        try:\n            fetched = await self._yf.get_stock_ohlcv(symbol, miss_start, miss_end)\n        except Exception as exc:\n            logger.error(\"yfinance failed for %s: %s\", symbol, _safe_str(exc))\n\n        if fetched:\n            await repository.insert_stock_ohlcv(self._db, fetched)\n\n    return await repository.get_stock_ohlcv(self._db, symbol, start, end)\n</code></pre>"},{"location":"reference/src/Kaare/client/#src.Kaare.client.KaareClient.get_stock_ohlcv(symbol)","title":"<code>symbol</code>","text":"(<code>str</code>)           \u2013            <p>Ticker symbol (e.g. <code>\"AAPL\"</code>).</p>"},{"location":"reference/src/Kaare/client/#src.Kaare.client.KaareClient.get_stock_ohlcv(start)","title":"<code>start</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive start date.</p>"},{"location":"reference/src/Kaare/client/#src.Kaare.client.KaareClient.get_stock_ohlcv(end)","title":"<code>end</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive end date.</p>"},{"location":"reference/src/Kaare/client/#src.Kaare.client.KaareClient.initialize","title":"initialize  <code>async</code>","text":"<pre><code>initialize() -&gt; None\n</code></pre> <p>Create the connection pool and run schema migrations.</p> <p>No-op when an external pool was supplied to the constructor.</p> Source code in <code>src/Kaare/client.py</code> <pre><code>async def initialize(self) -&gt; None:\n    \"\"\"Create the connection pool and run schema migrations.\n\n    No-op when an external pool was supplied to the constructor.\n    \"\"\"\n    if self._pool is None:\n        self._pool = await db_connection.create_pool()\n        await migrations.run_migrations(self._pool)\n</code></pre>"},{"location":"reference/src/Kaare/config/","title":"config","text":""},{"location":"reference/src/Kaare/config/#src.Kaare.config","title":"src.Kaare.config","text":"<p>Settings loaded from environment / .env file.</p>"},{"location":"reference/src/Kaare/db/","title":"db","text":""},{"location":"reference/src/Kaare/db/#src.Kaare.db","title":"src.Kaare.db","text":"<p>Database sub-package (connection pool, migrations, repository).</p> <p>Modules:</p> <ul> <li> <code>connection</code>           \u2013            <p>asyncpg connection pool creation.</p> </li> <li> <code>migrations</code>           \u2013            <p>Database schema migrations \u2014 run once at startup.</p> </li> <li> <code>repository</code>           \u2013            <p>Database read/write operations for stock_ohlcv and macro_data tables.</p> </li> </ul>"},{"location":"reference/src/Kaare/db/connection/","title":"connection","text":""},{"location":"reference/src/Kaare/db/connection/#src.Kaare.db.connection","title":"src.Kaare.db.connection","text":"<p>asyncpg connection pool creation.</p> <p>Functions:</p> <ul> <li> <code>close_shared_pool</code>             \u2013              <p>Close and discard the singleton pool (called at shutdown).</p> </li> <li> <code>create_pool</code>             \u2013              <p>Create and return a fresh asyncpg connection pool.</p> </li> <li> <code>get_shared_pool</code>             \u2013              <p>Return the process-wide singleton pool, creating it on first call.</p> </li> </ul>"},{"location":"reference/src/Kaare/db/connection/#src.Kaare.db.connection.close_shared_pool","title":"close_shared_pool  <code>async</code>","text":"<pre><code>close_shared_pool() -&gt; None\n</code></pre> <p>Close and discard the singleton pool (called at shutdown).</p> Source code in <code>src/Kaare/db/connection.py</code> <pre><code>async def close_shared_pool() -&gt; None:\n    \"\"\"Close and discard the singleton pool (called at shutdown).\"\"\"\n    global _shared_pool\n    if _shared_pool is not None:\n        await _shared_pool.close()\n        _shared_pool = None\n</code></pre>"},{"location":"reference/src/Kaare/db/connection/#src.Kaare.db.connection.create_pool","title":"create_pool  <code>async</code>","text":"<pre><code>create_pool() -&gt; asyncpg.Pool\n</code></pre> <p>Create and return a fresh asyncpg connection pool.</p> <p>Returns:</p> <ul> <li> <code>Pool</code>           \u2013            <p>A connected asyncpg pool ready for use.</p> </li> </ul> <p>Raises:</p> <ul> <li> <code>PostgresConnectionError</code>             \u2013            <p>If the database is unreachable.</p> </li> </ul> Source code in <code>src/Kaare/db/connection.py</code> <pre><code>async def create_pool() -&gt; asyncpg.Pool:\n    \"\"\"Create and return a fresh asyncpg connection pool.\n\n    Returns:\n        A connected asyncpg pool ready for use.\n\n    Raises:\n        asyncpg.PostgresConnectionError: If the database is unreachable.\n    \"\"\"\n    return await asyncpg.create_pool(\n        dsn=config.DB_DSN,\n        min_size=1,\n        max_size=10,\n    )\n</code></pre>"},{"location":"reference/src/Kaare/db/connection/#src.Kaare.db.connection.get_shared_pool","title":"get_shared_pool  <code>async</code>","text":"<pre><code>get_shared_pool() -&gt; asyncpg.Pool\n</code></pre> <p>Return the process-wide singleton pool, creating it on first call.</p> <p>Subsequent calls return the same pool without reconnecting.</p> Source code in <code>src/Kaare/db/connection.py</code> <pre><code>async def get_shared_pool() -&gt; asyncpg.Pool:\n    \"\"\"Return the process-wide singleton pool, creating it on first call.\n\n    Subsequent calls return the same pool without reconnecting.\n    \"\"\"\n    global _shared_pool\n    if _shared_pool is None:\n        _shared_pool = await create_pool()\n    return _shared_pool\n</code></pre>"},{"location":"reference/src/Kaare/db/migrations/","title":"migrations","text":""},{"location":"reference/src/Kaare/db/migrations/#src.Kaare.db.migrations","title":"src.Kaare.db.migrations","text":"<p>Database schema migrations \u2014 run once at startup.</p> <p>Functions:</p> <ul> <li> <code>run_migrations</code>             \u2013              <p>Create all required tables if they do not already exist.</p> </li> </ul>"},{"location":"reference/src/Kaare/db/migrations/#src.Kaare.db.migrations.run_migrations","title":"run_migrations  <code>async</code>","text":"<pre><code>run_migrations(pool: Pool) -&gt; None\n</code></pre> <p>Create all required tables if they do not already exist.</p> <p>Parameters:</p> Source code in <code>src/Kaare/db/migrations.py</code> <pre><code>async def run_migrations(pool: asyncpg.Pool) -&gt; None:\n    \"\"\"Create all required tables if they do not already exist.\n\n    Args:\n        pool: An active asyncpg connection pool.\n    \"\"\"\n    async with pool.acquire() as conn:\n        await conn.execute(_CREATE_STOCK_OHLCV)\n        await conn.execute(_CREATE_MACRO_DATA)\n        await conn.execute(_ADD_SENTIMENT_COLUMN)\n        await conn.execute(_ADD_VIX_COLUMN)\n        await conn.execute(_CREATE_RAW_NEWS)\n        await conn.execute(_CREATE_RAW_NEWS_INDEXES)\n        await conn.execute(_CREATE_ARTICLE_SENTIMENT)\n        await conn.execute(_CREATE_DAILY_MARKET_SENTIMENT)\n        await conn.execute(_CREATE_DAILY_TICKER_SENTIMENT)\n        await conn.execute(_MIGRATE_MODEL_VERSION)\n</code></pre>"},{"location":"reference/src/Kaare/db/migrations/#src.Kaare.db.migrations.run_migrations(pool)","title":"<code>pool</code>","text":"(<code>Pool</code>)           \u2013            <p>An active asyncpg connection pool.</p>"},{"location":"reference/src/Kaare/db/repository/","title":"repository","text":""},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository","title":"src.Kaare.db.repository","text":"<p>Database read/write operations for stock_ohlcv and macro_data tables.</p> <p>Functions:</p> <ul> <li> <code>aggregate_daily_market_sentiment</code>             \u2013              <p>Aggregate article-level scores into daily_market_sentiment.</p> </li> <li> <code>aggregate_daily_ticker_sentiment</code>             \u2013              <p>Aggregate article-level scores into daily_ticker_sentiment per ticker.</p> </li> <li> <code>get_article_sentiment_by_ids</code>             \u2013              <p>Fetch existing FinBERT scores for the given raw_news IDs.</p> </li> <li> <code>get_daily_ticker_sentiment</code>             \u2013              <p>Fetch pre-aggregated sentiment for symbol from daily_ticker_sentiment.</p> </li> <li> <code>get_dates_missing_sentiment</code>             \u2013              <p>Return dates in [start, end] that exist in macro_data but have no sentiment score.</p> </li> <li> <code>get_macro_data</code>             \u2013              <p>Fetch macro_data rows within [start, end] from the database.</p> </li> <li> <code>get_stock_ohlcv</code>             \u2013              <p>Fetch OHLCV rows for symbol within [start, end] from the database.</p> </li> <li> <code>get_ticker_sentiment_last_updated</code>             \u2013              <p>Return the most recent <code>updated_at</code> timestamp for symbol in daily_ticker_sentiment.</p> </li> <li> <code>get_unscored_article_batch</code>             \u2013              <p>Fetch articles that have not yet been scored by FinBERT.</p> </li> <li> <code>insert_article_sentiment_batch</code>             \u2013              <p>Insert FinBERT scores for a batch of articles, skipping duplicates.</p> </li> <li> <code>insert_raw_news_batch</code>             \u2013              <p>Insert a batch of raw news articles, skipping duplicates.</p> </li> <li> <code>insert_raw_news_returning_ids</code>             \u2013              <p>Insert raw news articles and return their database IDs.</p> </li> <li> <code>insert_stock_ohlcv</code>             \u2013              <p>Insert OHLCV records, ignoring conflicts on (symbol, date).</p> </li> <li> <code>sync_sentiment_to_macro</code>             \u2013              <p>Copy sentiment scores from daily_market_sentiment into macro_data.</p> </li> <li> <code>upsert_macro_data</code>             \u2013              <p>Upsert macro_data records (insert or update on date conflict).</p> </li> </ul>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.aggregate_daily_market_sentiment","title":"aggregate_daily_market_sentiment  <code>async</code>","text":"<pre><code>aggregate_daily_market_sentiment(pool: Pool) -&gt; int\n</code></pre> <p>Aggregate article-level scores into daily_market_sentiment.</p> <p>Computes mean, median, std, article count, pct_positive, pct_negative, and rolling 3-day and 7-day averages.</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>int</code>           \u2013            <p>Number of rows upserted.</p> </li> </ul> Source code in <code>src/Kaare/db/repository.py</code> <pre><code>async def aggregate_daily_market_sentiment(pool: asyncpg.Pool) -&gt; int:\n    \"\"\"Aggregate article-level scores into daily_market_sentiment.\n\n    Computes mean, median, std, article count, pct_positive, pct_negative,\n    and rolling 3-day and 7-day averages.\n\n    Args:\n        pool: Active asyncpg pool.\n\n    Returns:\n        Number of rows upserted.\n    \"\"\"\n    upsert_query = \"\"\"\n        INSERT INTO daily_market_sentiment\n            (trading_date, mean_score, median_score, std_score,\n             article_count, pct_positive, pct_negative)\n        SELECT\n            rn.trading_date,\n            AVG(s.net_score),\n            PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY s.net_score),\n            STDDEV(s.net_score),\n            COUNT(*),\n            AVG(CASE WHEN s.positive &gt; 0.5 THEN 1.0 ELSE 0.0 END),\n            AVG(CASE WHEN s.negative &gt; 0.5 THEN 1.0 ELSE 0.0 END)\n        FROM raw_news rn\n        JOIN article_sentiment s ON s.raw_news_id = rn.id\n        GROUP BY rn.trading_date\n        ON CONFLICT (trading_date) DO UPDATE SET\n            mean_score    = EXCLUDED.mean_score,\n            median_score  = EXCLUDED.median_score,\n            std_score     = EXCLUDED.std_score,\n            article_count = EXCLUDED.article_count,\n            pct_positive  = EXCLUDED.pct_positive,\n            pct_negative  = EXCLUDED.pct_negative,\n            updated_at    = NOW()\n    \"\"\"\n    rolling_query = \"\"\"\n        UPDATE daily_market_sentiment d SET\n            mean_score_3d = sub.avg_3d,\n            mean_score_7d = sub.avg_7d\n        FROM (\n            SELECT\n                trading_date,\n                AVG(mean_score) OVER (\n                    ORDER BY trading_date\n                    ROWS BETWEEN 2 PRECEDING AND CURRENT ROW\n                ) AS avg_3d,\n                AVG(mean_score) OVER (\n                    ORDER BY trading_date\n                    ROWS BETWEEN 6 PRECEDING AND CURRENT ROW\n                ) AS avg_7d\n            FROM daily_market_sentiment\n        ) sub\n        WHERE d.trading_date = sub.trading_date\n    \"\"\"\n    async with pool.acquire() as conn:\n        result = await conn.execute(upsert_query)\n        await conn.execute(rolling_query)\n    # result is like \"INSERT 0 N\"\n    try:\n        return int(result.split()[-1])\n    except (IndexError, ValueError):\n        return 0\n</code></pre>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.aggregate_daily_market_sentiment(pool)","title":"<code>pool</code>","text":"(<code>Pool</code>)           \u2013            <p>Active asyncpg pool.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.aggregate_daily_ticker_sentiment","title":"aggregate_daily_ticker_sentiment  <code>async</code>","text":"<pre><code>aggregate_daily_ticker_sentiment(pool: Pool) -&gt; int\n</code></pre> <p>Aggregate article-level scores into daily_ticker_sentiment per ticker.</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>int</code>           \u2013            <p>Number of rows upserted.</p> </li> </ul> Source code in <code>src/Kaare/db/repository.py</code> <pre><code>async def aggregate_daily_ticker_sentiment(pool: asyncpg.Pool) -&gt; int:\n    \"\"\"Aggregate article-level scores into daily_ticker_sentiment per ticker.\n\n    Args:\n        pool: Active asyncpg pool.\n\n    Returns:\n        Number of rows upserted.\n    \"\"\"\n    query = \"\"\"\n        INSERT INTO daily_ticker_sentiment\n            (trading_date, ticker, mean_score, article_count,\n             std_score, pct_positive, pct_negative)\n        SELECT\n            expanded.trading_date,\n            expanded.ticker,\n            AVG(expanded.net_score),\n            COUNT(*),\n            STDDEV(expanded.net_score),\n            AVG(CASE WHEN expanded.positive &gt; 0.5 THEN 1.0 ELSE 0.0 END),\n            AVG(CASE WHEN expanded.negative &gt; 0.5 THEN 1.0 ELSE 0.0 END)\n        FROM (\n            SELECT\n                rn.trading_date,\n                UNNEST(rn.tickers) AS ticker,\n                s.net_score,\n                s.positive,\n                s.negative\n            FROM raw_news rn\n            JOIN article_sentiment s ON s.raw_news_id = rn.id\n            WHERE rn.tickers IS NOT NULL AND array_length(rn.tickers, 1) &gt; 0\n        ) AS expanded\n        GROUP BY expanded.trading_date, expanded.ticker\n        ON CONFLICT (trading_date, ticker) DO UPDATE SET\n            mean_score    = EXCLUDED.mean_score,\n            article_count = EXCLUDED.article_count,\n            std_score     = EXCLUDED.std_score,\n            pct_positive  = EXCLUDED.pct_positive,\n            pct_negative  = EXCLUDED.pct_negative,\n            updated_at    = NOW()\n    \"\"\"\n    async with pool.acquire() as conn:\n        result = await conn.execute(query)\n    try:\n        return int(result.split()[-1])\n    except (IndexError, ValueError):\n        return 0\n</code></pre>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.aggregate_daily_ticker_sentiment(pool)","title":"<code>pool</code>","text":"(<code>Pool</code>)           \u2013            <p>Active asyncpg pool.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_article_sentiment_by_ids","title":"get_article_sentiment_by_ids  <code>async</code>","text":"<pre><code>get_article_sentiment_by_ids(pool: Pool, ids: list[int]) -&gt; dict[int, dict]\n</code></pre> <p>Fetch existing FinBERT scores for the given raw_news IDs.</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>dict[int, dict]</code>           \u2013            <p>Mapping of raw_news_id \u2192 score dict with keys positive, negative, neutral, net_score.</p> </li> </ul> Source code in <code>src/Kaare/db/repository.py</code> <pre><code>async def get_article_sentiment_by_ids(\n    pool: asyncpg.Pool,\n    ids: list[int],\n) -&gt; dict[int, dict]:\n    \"\"\"Fetch existing FinBERT scores for the given raw_news IDs.\n\n    Args:\n        pool: Active asyncpg pool.\n        ids: List of raw_news IDs to look up.\n\n    Returns:\n        Mapping of raw_news_id \u2192 score dict with keys positive, negative, neutral, net_score.\n    \"\"\"\n    if not ids:\n        return {}\n    query = \"\"\"\n        SELECT raw_news_id, positive, negative, neutral, net_score\n        FROM article_sentiment\n        WHERE raw_news_id = ANY($1)\n    \"\"\"\n    async with pool.acquire() as conn:\n        rows = await conn.fetch(query, ids)\n    return {\n        row[\"raw_news_id\"]: {\n            \"positive\": row[\"positive\"],\n            \"negative\": row[\"negative\"],\n            \"neutral\": row[\"neutral\"],\n            \"net_score\": row[\"net_score\"],\n        }\n        for row in rows\n    }\n</code></pre>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_article_sentiment_by_ids(pool)","title":"<code>pool</code>","text":"(<code>Pool</code>)           \u2013            <p>Active asyncpg pool.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_article_sentiment_by_ids(ids)","title":"<code>ids</code>","text":"(<code>list[int]</code>)           \u2013            <p>List of raw_news IDs to look up.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_daily_ticker_sentiment","title":"get_daily_ticker_sentiment  <code>async</code>","text":"<pre><code>get_daily_ticker_sentiment(pool: Pool, symbol: str, start: date, end: date) -&gt; list[tuple[datetime.date, float, int]]\n</code></pre> <p>Fetch pre-aggregated sentiment for symbol from daily_ticker_sentiment.</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>list[tuple[date, float, int]]</code>           \u2013            <p>List of <code>(trading_date, mean_score, article_count)</code> tuples ordered by date.</p> </li> </ul> Source code in <code>src/Kaare/db/repository.py</code> <pre><code>async def get_daily_ticker_sentiment(\n    pool: asyncpg.Pool,\n    symbol: str,\n    start: datetime.date,\n    end: datetime.date,\n) -&gt; list[tuple[datetime.date, float, int]]:\n    \"\"\"Fetch pre-aggregated sentiment for *symbol* from daily_ticker_sentiment.\n\n    Args:\n        pool: Active asyncpg pool.\n        symbol: Ticker symbol (case-insensitive).\n        start: Inclusive start date.\n        end: Inclusive end date.\n\n    Returns:\n        List of ``(trading_date, mean_score, article_count)`` tuples ordered by date.\n    \"\"\"\n    query = \"\"\"\n        SELECT trading_date, mean_score, article_count\n        FROM daily_ticker_sentiment\n        WHERE ticker = $1 AND trading_date BETWEEN $2 AND $3\n        ORDER BY trading_date\n    \"\"\"\n    async with pool.acquire() as conn:\n        rows = await conn.fetch(query, symbol.upper(), start, end)\n    return [(row[\"trading_date\"], row[\"mean_score\"], row[\"article_count\"]) for row in rows]\n</code></pre>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_daily_ticker_sentiment(pool)","title":"<code>pool</code>","text":"(<code>Pool</code>)           \u2013            <p>Active asyncpg pool.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_daily_ticker_sentiment(symbol)","title":"<code>symbol</code>","text":"(<code>str</code>)           \u2013            <p>Ticker symbol (case-insensitive).</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_daily_ticker_sentiment(start)","title":"<code>start</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive start date.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_daily_ticker_sentiment(end)","title":"<code>end</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive end date.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_dates_missing_sentiment","title":"get_dates_missing_sentiment  <code>async</code>","text":"<pre><code>get_dates_missing_sentiment(pool: Pool, start: date, end: date) -&gt; set[datetime.date]\n</code></pre> <p>Return dates in [start, end] that exist in macro_data but have no sentiment score.</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>set[date]</code>           \u2013            <p>Set of dates where <code>news_sentiment_score IS NULL</code>.</p> </li> </ul> Source code in <code>src/Kaare/db/repository.py</code> <pre><code>async def get_dates_missing_sentiment(\n    pool: asyncpg.Pool,\n    start: datetime.date,\n    end: datetime.date,\n) -&gt; set[datetime.date]:\n    \"\"\"Return dates in [start, end] that exist in macro_data but have no sentiment score.\n\n    Args:\n        pool: Active asyncpg pool.\n        start: Inclusive start date.\n        end: Inclusive end date.\n\n    Returns:\n        Set of dates where ``news_sentiment_score IS NULL``.\n    \"\"\"\n    query = \"\"\"\n        SELECT date FROM macro_data\n        WHERE date BETWEEN $1 AND $2 AND news_sentiment_score IS NULL\n    \"\"\"\n    async with pool.acquire() as conn:\n        rows = await conn.fetch(query, start, end)\n    return {row[\"date\"] for row in rows}\n</code></pre>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_dates_missing_sentiment(pool)","title":"<code>pool</code>","text":"(<code>Pool</code>)           \u2013            <p>Active asyncpg pool.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_dates_missing_sentiment(start)","title":"<code>start</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive start date.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_dates_missing_sentiment(end)","title":"<code>end</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive end date.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_macro_data","title":"get_macro_data  <code>async</code>","text":"<pre><code>get_macro_data(pool: Pool, start: date, end: date) -&gt; list[MacroData]\n</code></pre> <p>Fetch macro_data rows within [start, end] from the database.</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>list[MacroData]</code>           \u2013            <p>List of :class:<code>~Kaare.models.MacroData</code> ordered by date ascending.</p> </li> </ul> Source code in <code>src/Kaare/db/repository.py</code> <pre><code>async def get_macro_data(\n    pool: asyncpg.Pool,\n    start: datetime.date,\n    end: datetime.date,\n) -&gt; list[MacroData]:\n    \"\"\"Fetch macro_data rows within [start, end] from the database.\n\n    Args:\n        pool: Active asyncpg pool.\n        start: Inclusive start date.\n        end: Inclusive end date.\n\n    Returns:\n        List of :class:`~Kaare.models.MacroData` ordered by date ascending.\n    \"\"\"\n    query = \"\"\"\n        SELECT date, gold_price, treasury_yield_10y, vix, news_sentiment_score\n        FROM macro_data\n        WHERE date BETWEEN $1 AND $2\n        ORDER BY date\n    \"\"\"\n    async with pool.acquire() as conn:\n        rows = await conn.fetch(query, start, end)\n    return [\n        MacroData(\n            date=row[\"date\"],\n            gold_price=row[\"gold_price\"],\n            treasury_yield_10y=row[\"treasury_yield_10y\"],\n            vix=row[\"vix\"],\n            news_sentiment_score=row[\"news_sentiment_score\"],\n        )\n        for row in rows\n    ]\n</code></pre>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_macro_data(pool)","title":"<code>pool</code>","text":"(<code>Pool</code>)           \u2013            <p>Active asyncpg pool.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_macro_data(start)","title":"<code>start</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive start date.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_macro_data(end)","title":"<code>end</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive end date.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_stock_ohlcv","title":"get_stock_ohlcv  <code>async</code>","text":"<pre><code>get_stock_ohlcv(pool: Pool, symbol: str, start: date, end: date) -&gt; list[StockOHLCV]\n</code></pre> <p>Fetch OHLCV rows for symbol within [start, end] from the database.</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>list[StockOHLCV]</code>           \u2013            <p>List of :class:<code>~Kaare.models.StockOHLCV</code> ordered by date ascending.</p> </li> </ul> Source code in <code>src/Kaare/db/repository.py</code> <pre><code>async def get_stock_ohlcv(\n    pool: asyncpg.Pool,\n    symbol: str,\n    start: datetime.date,\n    end: datetime.date,\n) -&gt; list[StockOHLCV]:\n    \"\"\"Fetch OHLCV rows for *symbol* within [start, end] from the database.\n\n    Args:\n        pool: Active asyncpg pool.\n        symbol: Ticker symbol.\n        start: Inclusive start date.\n        end: Inclusive end date.\n\n    Returns:\n        List of :class:`~Kaare.models.StockOHLCV` ordered by date ascending.\n    \"\"\"\n    query = \"\"\"\n        SELECT symbol, date, open, high, low, close, volume\n        FROM stock_ohlcv\n        WHERE symbol = $1 AND date BETWEEN $2 AND $3\n        ORDER BY date\n    \"\"\"\n    async with pool.acquire() as conn:\n        rows = await conn.fetch(query, symbol, start, end)\n    return [\n        StockOHLCV(\n            symbol=row[\"symbol\"],\n            date=row[\"date\"],\n            open=row[\"open\"],\n            high=row[\"high\"],\n            low=row[\"low\"],\n            close=row[\"close\"],\n            volume=row[\"volume\"],\n        )\n        for row in rows\n    ]\n</code></pre>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_stock_ohlcv(pool)","title":"<code>pool</code>","text":"(<code>Pool</code>)           \u2013            <p>Active asyncpg pool.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_stock_ohlcv(symbol)","title":"<code>symbol</code>","text":"(<code>str</code>)           \u2013            <p>Ticker symbol.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_stock_ohlcv(start)","title":"<code>start</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive start date.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_stock_ohlcv(end)","title":"<code>end</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive end date.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_ticker_sentiment_last_updated","title":"get_ticker_sentiment_last_updated  <code>async</code>","text":"<pre><code>get_ticker_sentiment_last_updated(pool: Pool, symbol: str) -&gt; datetime.datetime | None\n</code></pre> <p>Return the most recent <code>updated_at</code> timestamp for symbol in daily_ticker_sentiment.</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>datetime | None</code>           \u2013            <p>The latest <code>updated_at</code> value, or <code>None</code> if no rows exist for this ticker.</p> </li> </ul> Source code in <code>src/Kaare/db/repository.py</code> <pre><code>async def get_ticker_sentiment_last_updated(\n    pool: asyncpg.Pool,\n    symbol: str,\n) -&gt; datetime.datetime | None:\n    \"\"\"Return the most recent ``updated_at`` timestamp for *symbol* in daily_ticker_sentiment.\n\n    Args:\n        pool: Active asyncpg pool.\n        symbol: Ticker symbol (case-insensitive).\n\n    Returns:\n        The latest ``updated_at`` value, or ``None`` if no rows exist for this ticker.\n    \"\"\"\n    query = \"\"\"\n        SELECT MAX(updated_at) AS last_updated\n        FROM daily_ticker_sentiment\n        WHERE ticker = $1\n    \"\"\"\n    async with pool.acquire() as conn:\n        row = await conn.fetchrow(query, symbol.upper())\n    return row[\"last_updated\"] if row else None\n</code></pre>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_ticker_sentiment_last_updated(pool)","title":"<code>pool</code>","text":"(<code>Pool</code>)           \u2013            <p>Active asyncpg pool.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_ticker_sentiment_last_updated(symbol)","title":"<code>symbol</code>","text":"(<code>str</code>)           \u2013            <p>Ticker symbol (case-insensitive).</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_unscored_article_batch","title":"get_unscored_article_batch  <code>async</code>","text":"<pre><code>get_unscored_article_batch(pool: Pool, limit: int) -&gt; list[tuple[int, str]]\n</code></pre> <p>Fetch articles that have not yet been scored by FinBERT.</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>list[tuple[int, str]]</code>           \u2013            <p>List of <code>(id, text)</code> tuples ordered by id.</p> </li> </ul> Source code in <code>src/Kaare/db/repository.py</code> <pre><code>async def get_unscored_article_batch(pool: asyncpg.Pool, limit: int) -&gt; list[tuple[int, str]]:\n    \"\"\"Fetch articles that have not yet been scored by FinBERT.\n\n    Args:\n        pool: Active asyncpg pool.\n        limit: Maximum number of rows to return.\n\n    Returns:\n        List of ``(id, text)`` tuples ordered by id.\n    \"\"\"\n    query = \"\"\"\n        SELECT rn.id, rn.text\n        FROM raw_news rn\n        LEFT JOIN article_sentiment s ON s.raw_news_id = rn.id\n        WHERE s.id IS NULL\n        ORDER BY rn.id\n        LIMIT $1\n    \"\"\"\n    async with pool.acquire() as conn:\n        rows = await conn.fetch(query, limit)\n    return [(row[\"id\"], row[\"text\"]) for row in rows]\n</code></pre>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_unscored_article_batch(pool)","title":"<code>pool</code>","text":"(<code>Pool</code>)           \u2013            <p>Active asyncpg pool.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.get_unscored_article_batch(limit)","title":"<code>limit</code>","text":"(<code>int</code>)           \u2013            <p>Maximum number of rows to return.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.insert_article_sentiment_batch","title":"insert_article_sentiment_batch  <code>async</code>","text":"<pre><code>insert_article_sentiment_batch(pool: Pool, data: list[tuple[int, dict]]) -&gt; None\n</code></pre> <p>Insert FinBERT scores for a batch of articles, skipping duplicates.</p> <p>Parameters:</p> Source code in <code>src/Kaare/db/repository.py</code> <pre><code>async def insert_article_sentiment_batch(\n    pool: asyncpg.Pool,\n    data: list[tuple[int, dict]],\n) -&gt; None:\n    \"\"\"Insert FinBERT scores for a batch of articles, skipping duplicates.\n\n    Args:\n        pool: Active asyncpg pool.\n        data: List of ``(raw_news_id, score_dict)`` where score_dict has keys\n            ``positive``, ``negative``, ``neutral``, ``net_score``.\n    \"\"\"\n    if not data:\n        return\n    query = \"\"\"\n        INSERT INTO article_sentiment (raw_news_id, positive, negative, neutral, net_score)\n        VALUES ($1, $2, $3, $4, $5)\n        ON CONFLICT (raw_news_id) DO NOTHING\n    \"\"\"\n    async with pool.acquire() as conn:\n        await conn.executemany(\n            query,\n            [\n                (raw_news_id, s[\"positive\"], s[\"negative\"], s[\"neutral\"], s[\"net_score\"])\n                for raw_news_id, s in data\n            ],\n        )\n</code></pre>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.insert_article_sentiment_batch(pool)","title":"<code>pool</code>","text":"(<code>Pool</code>)           \u2013            <p>Active asyncpg pool.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.insert_article_sentiment_batch(data)","title":"<code>data</code>","text":"(<code>list[tuple[int, dict]]</code>)           \u2013            <p>List of <code>(raw_news_id, score_dict)</code> where score_dict has keys <code>positive</code>, <code>negative</code>, <code>neutral</code>, <code>net_score</code>.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.insert_raw_news_batch","title":"insert_raw_news_batch  <code>async</code>","text":"<pre><code>insert_raw_news_batch(pool: Pool, records: list[RawNews]) -&gt; int\n</code></pre> <p>Insert a batch of raw news articles, skipping duplicates.</p> <p>Deduplication is based on <code>(text_hash, trading_date)</code>.</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>int</code>           \u2013            <p>Number of rows actually inserted.</p> </li> </ul> Source code in <code>src/Kaare/db/repository.py</code> <pre><code>async def insert_raw_news_batch(pool: asyncpg.Pool, records: list[RawNews]) -&gt; int:\n    \"\"\"Insert a batch of raw news articles, skipping duplicates.\n\n    Deduplication is based on ``(text_hash, trading_date)``.\n\n    Args:\n        pool: Active asyncpg pool.\n        records: Articles to insert.\n\n    Returns:\n        Number of rows actually inserted.\n    \"\"\"\n    if not records:\n        return 0\n    query = \"\"\"\n        INSERT INTO raw_news (date_utc, trading_date, text, text_hash, dataset_subset, source, tickers)\n        VALUES ($1, $2, $3, $4, $5, $6, $7)\n        ON CONFLICT (text_hash, trading_date) DO NOTHING\n        RETURNING id\n    \"\"\"\n    async with pool.acquire() as conn:\n        results = await conn.executemany(\n            query,\n            [\n                (r.date_utc, r.trading_date, r.text, r.text_hash, r.dataset_subset, r.source, r.tickers)\n                for r in records\n            ],\n        )\n    # executemany returns the status string; count RETURNING rows instead\n    # by checking affected rows via a simpler approach\n    return len(records)  # approximate; dedup happens silently\n</code></pre>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.insert_raw_news_batch(pool)","title":"<code>pool</code>","text":"(<code>Pool</code>)           \u2013            <p>Active asyncpg pool.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.insert_raw_news_batch(records)","title":"<code>records</code>","text":"(<code>list[RawNews]</code>)           \u2013            <p>Articles to insert.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.insert_raw_news_returning_ids","title":"insert_raw_news_returning_ids  <code>async</code>","text":"<pre><code>insert_raw_news_returning_ids(pool: Pool, records: list[RawNews]) -&gt; list[int]\n</code></pre> <p>Insert raw news articles and return their database IDs.</p> <p>Uses <code>ON CONFLICT DO UPDATE</code> (no-op) so the ID is always returned, whether the row was newly inserted or already existed.</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>list[int]</code>           \u2013            <p>List of IDs in the same order as records.</p> </li> </ul> Source code in <code>src/Kaare/db/repository.py</code> <pre><code>async def insert_raw_news_returning_ids(\n    pool: asyncpg.Pool,\n    records: list[RawNews],\n) -&gt; list[int]:\n    \"\"\"Insert raw news articles and return their database IDs.\n\n    Uses ``ON CONFLICT DO UPDATE`` (no-op) so the ID is always returned,\n    whether the row was newly inserted or already existed.\n\n    Args:\n        pool: Active asyncpg pool.\n        records: Articles to insert.\n\n    Returns:\n        List of IDs in the same order as *records*.\n    \"\"\"\n    if not records:\n        return []\n    # Single round-trip: bulk-upsert via UNNEST and return IDs in input order.\n    # tickers is TEXT[] in the DB; asyncpg can't encode list[list[str]] as text[][]\n    # reliably, so we pass the first ticker as a plain text and re-wrap in ARRAY[].\n    query = \"\"\"\n        WITH input AS (\n            SELECT\n                ordinality,\n                col1 AS date_utc,\n                col2 AS trading_date,\n                col3 AS text,\n                col4 AS text_hash,\n                col5 AS dataset_subset,\n                col6 AS source,\n                ARRAY[col7]::text[] AS tickers\n            FROM UNNEST(\n                $1::timestamptz[],\n                $2::date[],\n                $3::text[],\n                $4::text[],\n                $5::text[],\n                $6::text[],\n                $7::text[]\n            ) WITH ORDINALITY AS t(col1, col2, col3, col4, col5, col6, col7, ordinality)\n        ),\n        upserted AS (\n            INSERT INTO raw_news (date_utc, trading_date, text, text_hash, dataset_subset, source, tickers)\n            SELECT date_utc, trading_date, text, text_hash, dataset_subset, source, tickers\n            FROM input\n            ON CONFLICT (text_hash, trading_date) DO UPDATE SET text = EXCLUDED.text\n            RETURNING id, text_hash, trading_date\n        )\n        SELECT u.id\n        FROM input i\n        JOIN upserted u USING (text_hash, trading_date)\n        ORDER BY i.ordinality\n    \"\"\"\n    async with pool.acquire() as conn:\n        rows = await conn.fetch(\n            query,\n            [r.date_utc for r in records],\n            [r.trading_date for r in records],\n            [r.text for r in records],\n            [r.text_hash for r in records],\n            [r.dataset_subset for r in records],\n            [r.source for r in records],\n            [r.tickers[0] if r.tickers else \"\" for r in records],\n        )\n    return [row[\"id\"] for row in rows]\n</code></pre>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.insert_raw_news_returning_ids(pool)","title":"<code>pool</code>","text":"(<code>Pool</code>)           \u2013            <p>Active asyncpg pool.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.insert_raw_news_returning_ids(records)","title":"<code>records</code>","text":"(<code>list[RawNews]</code>)           \u2013            <p>Articles to insert.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.insert_stock_ohlcv","title":"insert_stock_ohlcv  <code>async</code>","text":"<pre><code>insert_stock_ohlcv(pool: Pool, records: list[StockOHLCV]) -&gt; None\n</code></pre> <p>Insert OHLCV records, ignoring conflicts on (symbol, date).</p> <p>Parameters:</p> Source code in <code>src/Kaare/db/repository.py</code> <pre><code>async def insert_stock_ohlcv(\n    pool: asyncpg.Pool,\n    records: list[StockOHLCV],\n) -&gt; None:\n    \"\"\"Insert OHLCV records, ignoring conflicts on (symbol, date).\n\n    Args:\n        pool: Active asyncpg pool.\n        records: Records to insert.\n    \"\"\"\n    if not records:\n        return\n    query = \"\"\"\n        INSERT INTO stock_ohlcv (symbol, date, open, high, low, close, volume)\n        VALUES ($1, $2, $3, $4, $5, $6, $7)\n        ON CONFLICT (symbol, date) DO NOTHING\n    \"\"\"\n    async with pool.acquire() as conn:\n        await conn.executemany(\n            query,\n            [\n                (r.symbol, r.date, r.open, r.high, r.low, r.close, r.volume)\n                for r in records\n            ],\n        )\n</code></pre>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.insert_stock_ohlcv(pool)","title":"<code>pool</code>","text":"(<code>Pool</code>)           \u2013            <p>Active asyncpg pool.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.insert_stock_ohlcv(records)","title":"<code>records</code>","text":"(<code>list[StockOHLCV]</code>)           \u2013            <p>Records to insert.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.sync_sentiment_to_macro","title":"sync_sentiment_to_macro  <code>async</code>","text":"<pre><code>sync_sentiment_to_macro(pool: Pool, start: date, end: date) -&gt; None\n</code></pre> <p>Copy sentiment scores from daily_market_sentiment into macro_data.</p> <p>Only updates rows where <code>news_sentiment_score</code> is currently <code>NULL</code>.</p> <p>Parameters:</p> Source code in <code>src/Kaare/db/repository.py</code> <pre><code>async def sync_sentiment_to_macro(\n    pool: asyncpg.Pool,\n    start: datetime.date,\n    end: datetime.date,\n) -&gt; None:\n    \"\"\"Copy sentiment scores from daily_market_sentiment into macro_data.\n\n    Only updates rows where ``news_sentiment_score`` is currently ``NULL``.\n\n    Args:\n        pool: Active asyncpg pool.\n        start: Inclusive start date.\n        end: Inclusive end date.\n    \"\"\"\n    query = \"\"\"\n        UPDATE macro_data m\n        SET news_sentiment_score = dms.mean_score\n        FROM daily_market_sentiment dms\n        WHERE m.date = dms.trading_date\n          AND m.date BETWEEN $1 AND $2\n          AND m.news_sentiment_score IS NULL\n    \"\"\"\n    async with pool.acquire() as conn:\n        await conn.execute(query, start, end)\n</code></pre>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.sync_sentiment_to_macro(pool)","title":"<code>pool</code>","text":"(<code>Pool</code>)           \u2013            <p>Active asyncpg pool.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.sync_sentiment_to_macro(start)","title":"<code>start</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive start date.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.sync_sentiment_to_macro(end)","title":"<code>end</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive end date.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.upsert_macro_data","title":"upsert_macro_data  <code>async</code>","text":"<pre><code>upsert_macro_data(pool: Pool, records: list[MacroData]) -&gt; None\n</code></pre> <p>Upsert macro_data records (insert or update on date conflict).</p> <p>Parameters:</p> Source code in <code>src/Kaare/db/repository.py</code> <pre><code>async def upsert_macro_data(\n    pool: asyncpg.Pool,\n    records: list[MacroData],\n) -&gt; None:\n    \"\"\"Upsert macro_data records (insert or update on date conflict).\n\n    Args:\n        pool: Active asyncpg pool.\n        records: Records to upsert.\n    \"\"\"\n    if not records:\n        return\n    query = \"\"\"\n        INSERT INTO macro_data (date, gold_price, treasury_yield_10y, vix, news_sentiment_score)\n        VALUES ($1, $2, $3, $4, $5)\n        ON CONFLICT (date) DO UPDATE\n            SET gold_price            = COALESCE(EXCLUDED.gold_price, macro_data.gold_price),\n                treasury_yield_10y    = COALESCE(EXCLUDED.treasury_yield_10y, macro_data.treasury_yield_10y),\n                vix                   = COALESCE(EXCLUDED.vix, macro_data.vix),\n                news_sentiment_score  = COALESCE(EXCLUDED.news_sentiment_score, macro_data.news_sentiment_score)\n    \"\"\"\n    async with pool.acquire() as conn:\n        await conn.executemany(\n            query,\n            [(r.date, r.gold_price, r.treasury_yield_10y, r.vix, r.news_sentiment_score) for r in records],\n        )\n</code></pre>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.upsert_macro_data(pool)","title":"<code>pool</code>","text":"(<code>Pool</code>)           \u2013            <p>Active asyncpg pool.</p>"},{"location":"reference/src/Kaare/db/repository/#src.Kaare.db.repository.upsert_macro_data(records)","title":"<code>records</code>","text":"(<code>list[MacroData]</code>)           \u2013            <p>Records to upsert.</p>"},{"location":"reference/src/Kaare/debug_hf/","title":"debug_hf","text":""},{"location":"reference/src/Kaare/debug_hf/#src.Kaare.debug_hf","title":"src.Kaare.debug_hf","text":"<p>Quick debug: print extra_fields for the first few rows of a subset.</p>"},{"location":"reference/src/Kaare/models/","title":"models","text":""},{"location":"reference/src/Kaare/models/#src.Kaare.models","title":"src.Kaare.models","text":"<p>Domain models for the Kaare data layer.</p> <p>Classes:</p> <ul> <li> <code>ArticleSentiment</code>           \u2013            <p>FinBERT sentiment scores for a single article.</p> </li> <li> <code>DailyMarketSentiment</code>           \u2013            <p>Aggregated market-wide sentiment for a single trading day.</p> </li> <li> <code>DailyTickerSentiment</code>           \u2013            <p>Aggregated per-ticker sentiment for a single trading day.</p> </li> <li> <code>MacroData</code>           \u2013            <p>Macro-economic data for a single date.</p> </li> <li> <code>NewsSentimentResult</code>           \u2013            <p>FinBERT sentiment result for a stock ticker over a date range.</p> </li> <li> <code>RawNews</code>           \u2013            <p>A single raw news article from the HuggingFace dataset.</p> </li> <li> <code>StockOHLCV</code>           \u2013            <p>OHLCV record for a single stock on a single date.</p> </li> </ul>"},{"location":"reference/src/Kaare/models/#src.Kaare.models.ArticleSentiment","title":"ArticleSentiment  <code>dataclass</code>","text":"<pre><code>ArticleSentiment(raw_news_id: int, positive: float, negative: float, neutral: float, net_score: float)\n</code></pre> <p>FinBERT sentiment scores for a single article.</p> <p>Attributes:</p> <ul> <li> <code>raw_news_id</code>               (<code>int</code>)           \u2013            <p>FK to <code>raw_news.id</code>.</p> </li> <li> <code>positive</code>               (<code>float</code>)           \u2013            <p>Probability of positive sentiment.</p> </li> <li> <code>negative</code>               (<code>float</code>)           \u2013            <p>Probability of negative sentiment.</p> </li> <li> <code>neutral</code>               (<code>float</code>)           \u2013            <p>Probability of neutral sentiment.</p> </li> <li> <code>net_score</code>               (<code>float</code>)           \u2013            <p><code>positive - negative</code>, range <code>[-1, 1]</code>.</p> </li> </ul>"},{"location":"reference/src/Kaare/models/#src.Kaare.models.DailyMarketSentiment","title":"DailyMarketSentiment  <code>dataclass</code>","text":"<pre><code>DailyMarketSentiment(trading_date: date, mean_score: float, median_score: float, article_count: int, std_score: float | None = None, pct_positive: float | None = None, pct_negative: float | None = None, mean_score_3d: float | None = None, mean_score_7d: float | None = None)\n</code></pre> <p>Aggregated market-wide sentiment for a single trading day.</p> <p>Attributes:</p> <ul> <li> <code>trading_date</code>               (<code>date</code>)           \u2013            <p>The trading day.</p> </li> <li> <code>mean_score</code>               (<code>float</code>)           \u2013            <p>Average net sentiment across all articles.</p> </li> <li> <code>median_score</code>               (<code>float</code>)           \u2013            <p>Median net sentiment.</p> </li> <li> <code>article_count</code>               (<code>int</code>)           \u2013            <p>Number of articles scored.</p> </li> <li> <code>std_score</code>               (<code>float | None</code>)           \u2013            <p>Standard deviation of net scores.</p> </li> <li> <code>pct_positive</code>               (<code>float | None</code>)           \u2013            <p>Fraction of articles with positive probability &gt; 0.5.</p> </li> <li> <code>pct_negative</code>               (<code>float | None</code>)           \u2013            <p>Fraction of articles with negative probability &gt; 0.5.</p> </li> <li> <code>mean_score_3d</code>               (<code>float | None</code>)           \u2013            <p>3-day rolling average of mean_score.</p> </li> <li> <code>mean_score_7d</code>               (<code>float | None</code>)           \u2013            <p>7-day rolling average of mean_score.</p> </li> </ul>"},{"location":"reference/src/Kaare/models/#src.Kaare.models.DailyTickerSentiment","title":"DailyTickerSentiment  <code>dataclass</code>","text":"<pre><code>DailyTickerSentiment(trading_date: date, ticker: str, mean_score: float, article_count: int, std_score: float | None = None, pct_positive: float | None = None, pct_negative: float | None = None)\n</code></pre> <p>Aggregated per-ticker sentiment for a single trading day.</p> <p>Attributes:</p> <ul> <li> <code>trading_date</code>               (<code>date</code>)           \u2013            <p>The trading day.</p> </li> <li> <code>ticker</code>               (<code>str</code>)           \u2013            <p>Ticker symbol.</p> </li> <li> <code>mean_score</code>               (<code>float</code>)           \u2013            <p>Average net sentiment for this ticker.</p> </li> <li> <code>article_count</code>               (<code>int</code>)           \u2013            <p>Number of articles tagged with this ticker.</p> </li> <li> <code>std_score</code>               (<code>float | None</code>)           \u2013            <p>Standard deviation of net scores.</p> </li> <li> <code>pct_positive</code>               (<code>float | None</code>)           \u2013            <p>Fraction of articles with positive probability &gt; 0.5.</p> </li> <li> <code>pct_negative</code>               (<code>float | None</code>)           \u2013            <p>Fraction of articles with negative probability &gt; 0.5.</p> </li> </ul>"},{"location":"reference/src/Kaare/models/#src.Kaare.models.MacroData","title":"MacroData  <code>dataclass</code>","text":"<pre><code>MacroData(date: date, gold_price: float | None, treasury_yield_10y: float | None, vix: float | None = None, news_sentiment_score: float | None = None)\n</code></pre> <p>Macro-economic data for a single date.</p> <p>Attributes:</p> <ul> <li> <code>date</code>               (<code>date</code>)           \u2013            <p>Date of the record.</p> </li> <li> <code>gold_price</code>               (<code>float | None</code>)           \u2013            <p>Spot gold price in USD, or <code>None</code> if unavailable.</p> </li> <li> <code>treasury_yield_10y</code>               (<code>float | None</code>)           \u2013            <p>10-year US Treasury yield (%), or <code>None</code> if unavailable.</p> </li> <li> <code>news_sentiment_score</code>               (<code>float | None</code>)           \u2013            <p>Daily market news sentiment in [-1.0, 1.0] computed by FinBERT, or <code>None</code> if not yet computed.</p> </li> </ul>"},{"location":"reference/src/Kaare/models/#src.Kaare.models.NewsSentimentResult","title":"NewsSentimentResult  <code>dataclass</code>","text":"<pre><code>NewsSentimentResult(symbol: str, start: date, end: date, article_count: int, avg_score: float, daily_scores: dict[date, float] = dict())\n</code></pre> <p>FinBERT sentiment result for a stock ticker over a date range.</p> <p>Attributes:</p> <ul> <li> <code>symbol</code>               (<code>str</code>)           \u2013            <p>Ticker symbol that was analysed.</p> </li> <li> <code>start</code>               (<code>date</code>)           \u2013            <p>Inclusive start date of the news window.</p> </li> <li> <code>end</code>               (<code>date</code>)           \u2013            <p>Inclusive end date of the news window.</p> </li> <li> <code>article_count</code>               (<code>int</code>)           \u2013            <p>Number of articles scored.</p> </li> <li> <code>avg_score</code>               (<code>float</code>)           \u2013            <p>Average FinBERT net sentiment (positive - negative) in [-1.0, 1.0].</p> </li> <li> <code>daily_scores</code>               (<code>dict[date, float]</code>)           \u2013            <p>Per-date average sentiment scores.</p> </li> </ul>"},{"location":"reference/src/Kaare/models/#src.Kaare.models.RawNews","title":"RawNews  <code>dataclass</code>","text":"<pre><code>RawNews(date_utc: datetime, trading_date: date, text: str, text_hash: str, dataset_subset: str, source: str | None, tickers: list[str] = list(), id: int | None = None)\n</code></pre> <p>A single raw news article from the HuggingFace dataset.</p> <p>Attributes:</p> <ul> <li> <code>date_utc</code>               (<code>datetime</code>)           \u2013            <p>Publication timestamp in UTC.</p> </li> <li> <code>trading_date</code>               (<code>date</code>)           \u2013            <p>NYSE-aligned trading date.</p> </li> <li> <code>text</code>               (<code>str</code>)           \u2013            <p>Article text (headline + body).</p> </li> <li> <code>text_hash</code>               (<code>str</code>)           \u2013            <p>SHA-256 of text for deduplication.</p> </li> <li> <code>dataset_subset</code>               (<code>str</code>)           \u2013            <p>Source subset name (e.g. <code>\"fnspid_news\"</code>).</p> </li> <li> <code>source</code>               (<code>str | None</code>)           \u2013            <p>Publisher name, or <code>None</code> if unknown.</p> </li> <li> <code>tickers</code>               (<code>list[str]</code>)           \u2013            <p>List of associated ticker symbols.</p> </li> <li> <code>id</code>               (<code>int | None</code>)           \u2013            <p>Database-assigned ID (<code>None</code> before insertion).</p> </li> </ul>"},{"location":"reference/src/Kaare/models/#src.Kaare.models.StockOHLCV","title":"StockOHLCV  <code>dataclass</code>","text":"<pre><code>StockOHLCV(symbol: str, date: date, open: float, high: float, low: float, close: float, volume: int)\n</code></pre> <p>OHLCV record for a single stock on a single date.</p> <p>Attributes:</p> <ul> <li> <code>symbol</code>               (<code>str</code>)           \u2013            <p>Ticker symbol (e.g. <code>\"AAPL\"</code>).</p> </li> <li> <code>date</code>               (<code>date</code>)           \u2013            <p>Trading date.</p> </li> <li> <code>open</code>               (<code>float</code>)           \u2013            <p>Opening price.</p> </li> <li> <code>high</code>               (<code>float</code>)           \u2013            <p>Daily high price.</p> </li> <li> <code>low</code>               (<code>float</code>)           \u2013            <p>Daily low price.</p> </li> <li> <code>close</code>               (<code>float</code>)           \u2013            <p>Adjusted closing price.</p> </li> <li> <code>volume</code>               (<code>int</code>)           \u2013            <p>Number of shares traded.</p> </li> </ul>"},{"location":"reference/src/Kaare/pipeline/","title":"pipeline","text":""},{"location":"reference/src/Kaare/pipeline/#src.Kaare.pipeline","title":"src.Kaare.pipeline","text":"<p>FinBERT sentiment pipeline \u2014 download, score, aggregate.</p> <p>Implements the three-step pipeline from the plan:</p> <ol> <li>Download \u2014 stream news from HuggingFace and store in <code>raw_news</code></li> <li>Score \u2014 run FinBERT on unscored articles and store in <code>article_sentiment</code></li> <li>Aggregate \u2014 compute daily market-wide and per-ticker sentiment tables</li> </ol> <p>Usage::</p> <pre><code>async with KaareClient() as client:\n    await run_pipeline(client._db)\n\n# Or run individual steps:\nawait download_news(pool, subsets=[\"sp500_daily_headlines\"], start=start, end=end)\nawait score_articles(pool)\nawait aggregate_daily(pool)\n</code></pre> <p>Functions:</p> <ul> <li> <code>aggregate_daily</code>             \u2013              <p>Aggregate article sentiment into daily market and ticker sentiment tables.</p> </li> <li> <code>download_news</code>             \u2013              <p>Stream news from HuggingFace and store in the <code>raw_news</code> table.</p> </li> <li> <code>run_pipeline</code>             \u2013              <p>Run the full FinBERT pipeline: download \u2192 score \u2192 aggregate.</p> </li> <li> <code>score_articles</code>             \u2013              <p>Run FinBERT on all unscored articles in <code>raw_news</code>.</p> </li> </ul>"},{"location":"reference/src/Kaare/pipeline/#src.Kaare.pipeline.aggregate_daily","title":"aggregate_daily  <code>async</code>","text":"<pre><code>aggregate_daily(pool: Pool) -&gt; None\n</code></pre> <p>Aggregate article sentiment into daily market and ticker sentiment tables.</p> <p>Populates <code>daily_market_sentiment</code> (including 3d/7d rolling averages) and <code>daily_ticker_sentiment</code>.</p> <p>Parameters:</p> Source code in <code>src/Kaare/pipeline.py</code> <pre><code>async def aggregate_daily(pool: asyncpg.Pool) -&gt; None:\n    \"\"\"Aggregate article sentiment into daily market and ticker sentiment tables.\n\n    Populates ``daily_market_sentiment`` (including 3d/7d rolling averages)\n    and ``daily_ticker_sentiment``.\n\n    Args:\n        pool: Active asyncpg pool.\n    \"\"\"\n    logger.info(\"Aggregating daily market-wide sentiment ...\")\n    n_market = await repository.aggregate_daily_market_sentiment(pool)\n    logger.info(\"  -&gt; %d trading days upserted.\", n_market)\n\n    logger.info(\"Aggregating daily per-ticker sentiment ...\")\n    n_ticker = await repository.aggregate_daily_ticker_sentiment(pool)\n    logger.info(\"  -&gt; %d ticker-day rows upserted.\", n_ticker)\n\n    logger.info(\"Aggregation complete.\")\n</code></pre>"},{"location":"reference/src/Kaare/pipeline/#src.Kaare.pipeline.aggregate_daily(pool)","title":"<code>pool</code>","text":"(<code>Pool</code>)           \u2013            <p>Active asyncpg pool.</p>"},{"location":"reference/src/Kaare/pipeline/#src.Kaare.pipeline.download_news","title":"download_news  <code>async</code>","text":"<pre><code>download_news(pool: Pool, subsets: list[str], start: date, end: date, batch_size: int = 1000) -&gt; int\n</code></pre> <p>Stream news from HuggingFace and store in the <code>raw_news</code> table.</p> <p>Runs the HuggingFace dataset iterator in a thread executor so the async event loop is never blocked. Each yielded batch is inserted immediately.</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>int</code>           \u2013            <p>Approximate total records inserted (duplicates are silently skipped).</p> </li> </ul> Source code in <code>src/Kaare/pipeline.py</code> <pre><code>async def download_news(\n    pool: asyncpg.Pool,\n    subsets: list[str],\n    start: datetime.date,\n    end: datetime.date,\n    batch_size: int = 1000,\n) -&gt; int:\n    \"\"\"Stream news from HuggingFace and store in the ``raw_news`` table.\n\n    Runs the HuggingFace dataset iterator in a thread executor so the async\n    event loop is never blocked. Each yielded batch is inserted immediately.\n\n    Args:\n        pool: Active asyncpg pool.\n        subsets: Dataset subset names to download.\n        start: Inclusive start date filter.\n        end: Inclusive end date filter.\n        batch_size: Records per insert batch.\n\n    Returns:\n        Approximate total records inserted (duplicates are silently skipped).\n    \"\"\"\n    provider = HFNewsProvider()\n    loop = asyncio.get_running_loop()\n    gen = provider.stream_batches(subsets, start, end, batch_size)\n\n    _sentinel = object()\n\n    def _next_batch():\n        try:\n            return next(gen)\n        except StopIteration:\n            return _sentinel\n\n    total = 0\n    while True:\n        batch = await loop.run_in_executor(None, _next_batch)\n        if batch is _sentinel:\n            break\n        await repository.insert_raw_news_batch(pool, batch)\n        total += len(batch)\n        logger.info(\"  raw_news: ~%d rows processed so far ...\", total)\n\n    logger.info(\"Download complete. ~%d total rows processed.\", total)\n    return total\n</code></pre>"},{"location":"reference/src/Kaare/pipeline/#src.Kaare.pipeline.download_news(pool)","title":"<code>pool</code>","text":"(<code>Pool</code>)           \u2013            <p>Active asyncpg pool.</p>"},{"location":"reference/src/Kaare/pipeline/#src.Kaare.pipeline.download_news(subsets)","title":"<code>subsets</code>","text":"(<code>list[str]</code>)           \u2013            <p>Dataset subset names to download.</p>"},{"location":"reference/src/Kaare/pipeline/#src.Kaare.pipeline.download_news(start)","title":"<code>start</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive start date filter.</p>"},{"location":"reference/src/Kaare/pipeline/#src.Kaare.pipeline.download_news(end)","title":"<code>end</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive end date filter.</p>"},{"location":"reference/src/Kaare/pipeline/#src.Kaare.pipeline.download_news(batch_size)","title":"<code>batch_size</code>","text":"(<code>int</code>, default:                   <code>1000</code> )           \u2013            <p>Records per insert batch.</p>"},{"location":"reference/src/Kaare/pipeline/#src.Kaare.pipeline.run_pipeline","title":"run_pipeline  <code>async</code>","text":"<pre><code>run_pipeline(pool: Pool, subsets: list[str] | None = None, start: date | None = None, end: date | None = None) -&gt; None\n</code></pre> <p>Run the full FinBERT pipeline: download \u2192 score \u2192 aggregate.</p> <p>Safe to re-run \u2014 all steps use <code>ON CONFLICT DO NOTHING/UPDATE</code> so existing data is never duplicated.</p> <p>Parameters:</p> Source code in <code>src/Kaare/pipeline.py</code> <pre><code>async def run_pipeline(\n    pool: asyncpg.Pool,\n    subsets: list[str] | None = None,\n    start: datetime.date | None = None,\n    end: datetime.date | None = None,\n) -&gt; None:\n    \"\"\"Run the full FinBERT pipeline: download \u2192 score \u2192 aggregate.\n\n    Safe to re-run \u2014 all steps use ``ON CONFLICT DO NOTHING/UPDATE`` so\n    existing data is never duplicated.\n\n    Args:\n        pool: Active asyncpg pool.\n        subsets: HuggingFace dataset subsets to use. Defaults to\n            ``config.FINANCE_SUBSETS``.\n        start: Inclusive start date. Defaults to 3 years ago.\n        end: Inclusive end date. Defaults to today.\n    \"\"\"\n    today = datetime.date.today()\n    start = start or (today - datetime.timedelta(days=365 * 3))\n    end = end or today\n    subsets = subsets or config.FINANCE_SUBSETS\n\n    logger.info(\"FinBERT pipeline: %s \u2192 %s | subsets: %s\", start, end, subsets)\n\n    await download_news(pool, subsets, start, end)\n    await score_articles(pool)\n    await aggregate_daily(pool)\n\n    logger.info(\"Pipeline complete.\")\n</code></pre>"},{"location":"reference/src/Kaare/pipeline/#src.Kaare.pipeline.run_pipeline(pool)","title":"<code>pool</code>","text":"(<code>Pool</code>)           \u2013            <p>Active asyncpg pool.</p>"},{"location":"reference/src/Kaare/pipeline/#src.Kaare.pipeline.run_pipeline(subsets)","title":"<code>subsets</code>","text":"(<code>list[str] | None</code>, default:                   <code>None</code> )           \u2013            <p>HuggingFace dataset subsets to use. Defaults to <code>config.FINANCE_SUBSETS</code>.</p>"},{"location":"reference/src/Kaare/pipeline/#src.Kaare.pipeline.run_pipeline(start)","title":"<code>start</code>","text":"(<code>date | None</code>, default:                   <code>None</code> )           \u2013            <p>Inclusive start date. Defaults to 3 years ago.</p>"},{"location":"reference/src/Kaare/pipeline/#src.Kaare.pipeline.run_pipeline(end)","title":"<code>end</code>","text":"(<code>date | None</code>, default:                   <code>None</code> )           \u2013            <p>Inclusive end date. Defaults to today.</p>"},{"location":"reference/src/Kaare/pipeline/#src.Kaare.pipeline.score_articles","title":"score_articles  <code>async</code>","text":"<pre><code>score_articles(pool: Pool, batch_size: int | None = None) -&gt; int\n</code></pre> <p>Run FinBERT on all unscored articles in <code>raw_news</code>.</p> <p>Fetches articles in chunks, runs inference in GPU/CPU batch_size sub-batches, and stores results in <code>article_sentiment</code>. Falls back to per-article scoring if a batch fails.</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>int</code>           \u2013            <p>Total articles scored in this run.</p> </li> </ul> Source code in <code>src/Kaare/pipeline.py</code> <pre><code>async def score_articles(\n    pool: asyncpg.Pool,\n    batch_size: int | None = None,\n) -&gt; int:\n    \"\"\"Run FinBERT on all unscored articles in ``raw_news``.\n\n    Fetches articles in chunks, runs inference in GPU/CPU batch_size sub-batches,\n    and stores results in ``article_sentiment``. Falls back to per-article scoring\n    if a batch fails.\n\n    Args:\n        pool: Active asyncpg pool.\n        batch_size: FinBERT inference batch size. Defaults to ``config.BATCH_SIZE``.\n\n    Returns:\n        Total articles scored in this run.\n    \"\"\"\n    bs = batch_size or config.BATCH_SIZE\n    analyzer = FinBERTAnalyzer()\n    total = 0\n\n    while True:\n        # Fetch a larger chunk from DB, process in smaller inference batches\n        rows = await repository.get_unscored_article_batch(pool, bs * 10)\n        if not rows:\n            break\n\n        for i in range(0, len(rows), bs):\n            chunk = rows[i : i + bs]\n            ids = [r[0] for r in chunk]\n            texts = [r[1] for r in chunk]\n\n            try:\n                scores = await analyzer.score_articles(texts)\n            except Exception as exc:\n                logger.warning(\n                    \"Batch scoring failed, falling back to per-article: %s\",\n                    _safe_str(exc),\n                )\n                scores = []\n                for text in texts:\n                    try:\n                        s = await analyzer.score_articles([text])\n                        scores.extend(s)\n                    except Exception as inner:\n                        logger.warning(\n                            \"Skipping article (scoring error): %s\", _safe_str(inner)\n                        )\n                        scores.append(\n                            {\n                                \"positive\": 0.0,\n                                \"negative\": 0.0,\n                                \"neutral\": 1.0,\n                                \"net_score\": 0.0,\n                            }\n                        )\n\n            await repository.insert_article_sentiment_batch(\n                pool, list(zip(ids, scores))\n            )\n            total += len(chunk)\n\n        logger.info(\"  Scored %d articles so far ...\", total)\n\n    logger.info(\"Scoring complete. Total scored: %d\", total)\n    return total\n</code></pre>"},{"location":"reference/src/Kaare/pipeline/#src.Kaare.pipeline.score_articles(pool)","title":"<code>pool</code>","text":"(<code>Pool</code>)           \u2013            <p>Active asyncpg pool.</p>"},{"location":"reference/src/Kaare/pipeline/#src.Kaare.pipeline.score_articles(batch_size)","title":"<code>batch_size</code>","text":"(<code>int | None</code>, default:                   <code>None</code> )           \u2013            <p>FinBERT inference batch size. Defaults to <code>config.BATCH_SIZE</code>.</p>"},{"location":"reference/src/Kaare/providers/","title":"providers","text":""},{"location":"reference/src/Kaare/providers/#src.Kaare.providers","title":"src.Kaare.providers","text":"<p>Data provider sub-package (yfinance).</p> <p>Modules:</p> <ul> <li> <code>base</code>           \u2013            <p>Abstract base class for data providers.</p> </li> <li> <code>finnhub_provider</code>           \u2013            <p>Finnhub news provider \u2014 fetches company news headlines for FinBERT scoring.</p> </li> <li> <code>hf_provider</code>           \u2013            <p>HuggingFace financial news dataset provider.</p> </li> <li> <code>yfinance_provider</code>           \u2013            <p>yfinance-based fallback data provider (runs in executor to avoid blocking).</p> </li> </ul>"},{"location":"reference/src/Kaare/providers/base/","title":"base","text":""},{"location":"reference/src/Kaare/providers/base/#src.Kaare.providers.base","title":"src.Kaare.providers.base","text":"<p>Abstract base class for data providers.</p> <p>Classes:</p> <ul> <li> <code>DataProvider</code>           \u2013            <p>Interface that every concrete data provider must implement.</p> </li> </ul>"},{"location":"reference/src/Kaare/providers/base/#src.Kaare.providers.base.DataProvider","title":"DataProvider","text":"<p>               Bases: <code>ABC</code></p> <p>Interface that every concrete data provider must implement.</p> <p>Methods:</p> <ul> <li> <code>get_gold_prices</code>             \u2013              <p>Fetch daily spot gold prices within [start, end].</p> </li> <li> <code>get_stock_ohlcv</code>             \u2013              <p>Fetch OHLCV data for symbol within [start, end].</p> </li> <li> <code>get_treasury_yields</code>             \u2013              <p>Fetch daily 10-year Treasury yields within [start, end].</p> </li> </ul>"},{"location":"reference/src/Kaare/providers/base/#src.Kaare.providers.base.DataProvider.get_gold_prices","title":"get_gold_prices  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>get_gold_prices(start: date, end: date) -&gt; dict[datetime.date, float]\n</code></pre> <p>Fetch daily spot gold prices within [start, end].</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>dict[date, float]</code>           \u2013            <p>Mapping of date \u2192 gold price (USD).</p> </li> </ul> Source code in <code>src/Kaare/providers/base.py</code> <pre><code>@abstractmethod\nasync def get_gold_prices(\n    self,\n    start: datetime.date,\n    end: datetime.date,\n) -&gt; dict[datetime.date, float]:\n    \"\"\"Fetch daily spot gold prices within [start, end].\n\n    Args:\n        start: Inclusive start date.\n        end: Inclusive end date.\n\n    Returns:\n        Mapping of date \u2192 gold price (USD).\n    \"\"\"\n</code></pre>"},{"location":"reference/src/Kaare/providers/base/#src.Kaare.providers.base.DataProvider.get_gold_prices(start)","title":"<code>start</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive start date.</p>"},{"location":"reference/src/Kaare/providers/base/#src.Kaare.providers.base.DataProvider.get_gold_prices(end)","title":"<code>end</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive end date.</p>"},{"location":"reference/src/Kaare/providers/base/#src.Kaare.providers.base.DataProvider.get_stock_ohlcv","title":"get_stock_ohlcv  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>get_stock_ohlcv(symbol: str, start: date, end: date) -&gt; list[StockOHLCV]\n</code></pre> <p>Fetch OHLCV data for symbol within [start, end].</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>list[StockOHLCV]</code>           \u2013            <p>List of :class:<code>~Kaare.models.StockOHLCV</code> records.</p> </li> </ul> Source code in <code>src/Kaare/providers/base.py</code> <pre><code>@abstractmethod\nasync def get_stock_ohlcv(\n    self,\n    symbol: str,\n    start: datetime.date,\n    end: datetime.date,\n) -&gt; list[StockOHLCV]:\n    \"\"\"Fetch OHLCV data for *symbol* within [start, end].\n\n    Args:\n        symbol: Ticker symbol.\n        start: Inclusive start date.\n        end: Inclusive end date.\n\n    Returns:\n        List of :class:`~Kaare.models.StockOHLCV` records.\n    \"\"\"\n</code></pre>"},{"location":"reference/src/Kaare/providers/base/#src.Kaare.providers.base.DataProvider.get_stock_ohlcv(symbol)","title":"<code>symbol</code>","text":"(<code>str</code>)           \u2013            <p>Ticker symbol.</p>"},{"location":"reference/src/Kaare/providers/base/#src.Kaare.providers.base.DataProvider.get_stock_ohlcv(start)","title":"<code>start</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive start date.</p>"},{"location":"reference/src/Kaare/providers/base/#src.Kaare.providers.base.DataProvider.get_stock_ohlcv(end)","title":"<code>end</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive end date.</p>"},{"location":"reference/src/Kaare/providers/base/#src.Kaare.providers.base.DataProvider.get_treasury_yields","title":"get_treasury_yields  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>get_treasury_yields(start: date, end: date) -&gt; dict[datetime.date, float]\n</code></pre> <p>Fetch daily 10-year Treasury yields within [start, end].</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>dict[date, float]</code>           \u2013            <p>Mapping of date \u2192 yield (%).</p> </li> </ul> Source code in <code>src/Kaare/providers/base.py</code> <pre><code>@abstractmethod\nasync def get_treasury_yields(\n    self,\n    start: datetime.date,\n    end: datetime.date,\n) -&gt; dict[datetime.date, float]:\n    \"\"\"Fetch daily 10-year Treasury yields within [start, end].\n\n    Args:\n        start: Inclusive start date.\n        end: Inclusive end date.\n\n    Returns:\n        Mapping of date \u2192 yield (%).\n    \"\"\"\n</code></pre>"},{"location":"reference/src/Kaare/providers/base/#src.Kaare.providers.base.DataProvider.get_treasury_yields(start)","title":"<code>start</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive start date.</p>"},{"location":"reference/src/Kaare/providers/base/#src.Kaare.providers.base.DataProvider.get_treasury_yields(end)","title":"<code>end</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive end date.</p>"},{"location":"reference/src/Kaare/providers/finnhub_provider/","title":"finnhub_provider","text":""},{"location":"reference/src/Kaare/providers/finnhub_provider/#src.Kaare.providers.finnhub_provider","title":"src.Kaare.providers.finnhub_provider","text":"<p>Finnhub news provider \u2014 fetches company news headlines for FinBERT scoring.</p> <p>Classes:</p> <ul> <li> <code>FinnhubProvider</code>           \u2013            <p>Fetches company news headlines from the Finnhub API.</p> </li> </ul>"},{"location":"reference/src/Kaare/providers/finnhub_provider/#src.Kaare.providers.finnhub_provider.FinnhubProvider","title":"FinnhubProvider","text":"<pre><code>FinnhubProvider()\n</code></pre> <p>Fetches company news headlines from the Finnhub API.</p> <p>All blocking SDK calls are offloaded to a thread-pool executor so the async event loop is never blocked.</p> <p>Methods:</p> <ul> <li> <code>fetch_raw_news</code>             \u2013              <p>Fetch news for symbol and return fully-populated :class:<code>~Kaare.models.RawNews</code> objects.</p> </li> <li> <code>get_news_by_date</code>             \u2013              <p>Fetch news for symbols and group headline+summary texts by date.</p> </li> </ul> Source code in <code>src/Kaare/providers/finnhub_provider.py</code> <pre><code>def __init__(self) -&gt; None:\n    if not config.FINNHUB_API_KEY:\n        raise RuntimeError(\"FINNHUB_API_KEY is not set. Check your .env file.\")\n    self._client = finnhub.Client(api_key=config.FINNHUB_API_KEY)\n</code></pre>"},{"location":"reference/src/Kaare/providers/finnhub_provider/#src.Kaare.providers.finnhub_provider.FinnhubProvider.fetch_raw_news","title":"fetch_raw_news  <code>async</code>","text":"<pre><code>fetch_raw_news(symbol: str, start: date, end: date) -&gt; list[RawNews]\n</code></pre> <p>Fetch news for symbol and return fully-populated :class:<code>~Kaare.models.RawNews</code> objects.</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>list[RawNews]</code>           \u2013            <p>List of :class:<code>~Kaare.models.RawNews</code> ready for DB insertion.</p> </li> </ul> Source code in <code>src/Kaare/providers/finnhub_provider.py</code> <pre><code>async def fetch_raw_news(\n    self,\n    symbol: str,\n    start: datetime.date,\n    end: datetime.date,\n) -&gt; list[RawNews]:\n    \"\"\"Fetch news for *symbol* and return fully-populated :class:`~Kaare.models.RawNews` objects.\n\n    Args:\n        symbol: Ticker symbol.\n        start: Inclusive start date.\n        end: Inclusive end date.\n\n    Returns:\n        List of :class:`~Kaare.models.RawNews` ready for DB insertion.\n    \"\"\"\n    loop = asyncio.get_running_loop()\n    try:\n        return await loop.run_in_executor(\n            None, partial(self._fetch_raw_news_sync, symbol, start, end)\n        )\n    except Exception as exc:\n        logger.warning(\"Finnhub fetch_raw_news failed for %s: %s\", symbol, exc)\n        return []\n</code></pre>"},{"location":"reference/src/Kaare/providers/finnhub_provider/#src.Kaare.providers.finnhub_provider.FinnhubProvider.fetch_raw_news(symbol)","title":"<code>symbol</code>","text":"(<code>str</code>)           \u2013            <p>Ticker symbol.</p>"},{"location":"reference/src/Kaare/providers/finnhub_provider/#src.Kaare.providers.finnhub_provider.FinnhubProvider.fetch_raw_news(start)","title":"<code>start</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive start date.</p>"},{"location":"reference/src/Kaare/providers/finnhub_provider/#src.Kaare.providers.finnhub_provider.FinnhubProvider.fetch_raw_news(end)","title":"<code>end</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive end date.</p>"},{"location":"reference/src/Kaare/providers/finnhub_provider/#src.Kaare.providers.finnhub_provider.FinnhubProvider.get_news_by_date","title":"get_news_by_date  <code>async</code>","text":"<pre><code>get_news_by_date(symbols: list[str], start: date, end: date) -&gt; dict[datetime.date, list[str]]\n</code></pre> <p>Fetch news for symbols and group headline+summary texts by date.</p> <p>For each symbol, one Finnhub API call is made covering the full [start, end] range. Articles are then bucketed by their publish date.</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>dict[date, list[str]]</code>           \u2013            <p>Mapping of date \u2192 list of article texts (<code>\"headline. summary\"</code>).</p> </li> </ul> Source code in <code>src/Kaare/providers/finnhub_provider.py</code> <pre><code>async def get_news_by_date(\n    self,\n    symbols: list[str],\n    start: datetime.date,\n    end: datetime.date,\n) -&gt; dict[datetime.date, list[str]]:\n    \"\"\"Fetch news for *symbols* and group headline+summary texts by date.\n\n    For each symbol, one Finnhub API call is made covering the full\n    [start, end] range. Articles are then bucketed by their publish date.\n\n    Args:\n        symbols: Ticker symbols to fetch news for.\n        start: Inclusive start date.\n        end: Inclusive end date.\n\n    Returns:\n        Mapping of date \u2192 list of article texts (``\"headline. summary\"``).\n    \"\"\"\n    loop = asyncio.get_running_loop()\n    by_date: dict[datetime.date, list[str]] = {}\n\n    for symbol in symbols:\n        try:\n            articles = await loop.run_in_executor(\n                None,\n                partial(self._fetch_company_news_sync, symbol, start, end),\n            )\n        except Exception as exc:\n            logger.warning(\"Finnhub news failed for %s: %s\", symbol, exc)\n            continue\n\n        for article in articles:\n            ts = article.get(\"datetime\", 0)\n            date = datetime.date.fromtimestamp(ts)\n            if not (start &lt;= date &lt;= end):\n                continue\n            headline = _clean_text(article.get(\"headline\", \"\"))\n            summary = _clean_text(article.get(\"summary\", \"\"))\n            text = f\"{headline}. {summary}\".strip(\". \")\n            if text:\n                by_date.setdefault(date, []).append(text)\n\n    return by_date\n</code></pre>"},{"location":"reference/src/Kaare/providers/finnhub_provider/#src.Kaare.providers.finnhub_provider.FinnhubProvider.get_news_by_date(symbols)","title":"<code>symbols</code>","text":"(<code>list[str]</code>)           \u2013            <p>Ticker symbols to fetch news for.</p>"},{"location":"reference/src/Kaare/providers/finnhub_provider/#src.Kaare.providers.finnhub_provider.FinnhubProvider.get_news_by_date(start)","title":"<code>start</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive start date.</p>"},{"location":"reference/src/Kaare/providers/finnhub_provider/#src.Kaare.providers.finnhub_provider.FinnhubProvider.get_news_by_date(end)","title":"<code>end</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive end date.</p>"},{"location":"reference/src/Kaare/providers/hf_provider/","title":"hf_provider","text":""},{"location":"reference/src/Kaare/providers/hf_provider/#src.Kaare.providers.hf_provider","title":"src.Kaare.providers.hf_provider","text":"<p>HuggingFace financial news dataset provider.</p> <p>Streams articles from <code>Brianferrell787/financial-news-multisource</code> and yields batches of :class:<code>~Kaare.models.RawNews</code> objects for insertion into the <code>raw_news</code> table.</p> <p>Classes:</p> <ul> <li> <code>HFNewsProvider</code>           \u2013            <p>Streams financial news from HuggingFace for FinBERT ingestion.</p> </li> </ul>"},{"location":"reference/src/Kaare/providers/hf_provider/#src.Kaare.providers.hf_provider.HFNewsProvider","title":"HFNewsProvider","text":"<p>Streams financial news from HuggingFace for FinBERT ingestion.</p> <p>Example::</p> <pre><code>provider = HFNewsProvider()\nfor batch in provider.stream_batches([\"sp500_daily_headlines\"], start, end):\n    await repository.insert_raw_news_batch(pool, batch)\n</code></pre> <p>Methods:</p> <ul> <li> <code>stream_batches</code>             \u2013              <p>Stream and parse news articles, yielding batches of :class:<code>~Kaare.models.RawNews</code>.</p> </li> </ul>"},{"location":"reference/src/Kaare/providers/hf_provider/#src.Kaare.providers.hf_provider.HFNewsProvider.stream_batches","title":"stream_batches","text":"<pre><code>stream_batches(subsets: list[str], start: date, end: date, batch_size: int = 1000) -&gt; Generator[list[RawNews], None, None]\n</code></pre> <p>Stream and parse news articles, yielding batches of :class:<code>~Kaare.models.RawNews</code>.</p> <p>This is a synchronous generator \u2014 call it from a thread executor.</p> <p>Parameters:</p> <p>Yields:</p> <ul> <li> <code>list[RawNews]</code>           \u2013            <p>Batches of parsed :class:<code>~Kaare.models.RawNews</code> objects.</p> </li> </ul> Source code in <code>src/Kaare/providers/hf_provider.py</code> <pre><code>def stream_batches(\n    self,\n    subsets: list[str],\n    start: datetime.date,\n    end: datetime.date,\n    batch_size: int = 1000,\n) -&gt; Generator[list[RawNews], None, None]:\n    \"\"\"Stream and parse news articles, yielding batches of :class:`~Kaare.models.RawNews`.\n\n    This is a **synchronous** generator \u2014 call it from a thread executor.\n\n    Args:\n        subsets: Dataset subset names to load (e.g. ``[\"sp500_daily_headlines\"]``).\n        start: Inclusive start date filter.\n        end: Inclusive end date filter.\n        batch_size: Number of records per yielded batch.\n\n    Yields:\n        Batches of parsed :class:`~Kaare.models.RawNews` objects.\n    \"\"\"\n    from datasets import load_dataset  # lazy import \u2014 heavy dependency\n\n    data_files = [f\"data/{s}/*.parquet\" for s in subsets]\n    logger.info(\"Streaming %d HuggingFace subset(s): %s\", len(subsets), subsets)\n\n    ds = load_dataset(\n        _DATASET_NAME,\n        data_files=data_files,\n        split=\"train\",\n        streaming=True,\n        token=config.HF_TOKEN or None,\n    )\n\n    batch: list[RawNews] = []\n    skipped = 0\n\n    for row in ds:\n        text = row.get(\"text\", \"\")\n        if not text or len(text.strip()) &lt; 20:\n            skipped += 1\n            continue\n\n        extras = _parse_extras(row)\n        trading_date = _parse_trading_date(row, extras)\n        if trading_date is None or not (start &lt;= trading_date &lt;= end):\n            skipped += 1\n            continue\n\n        batch.append(\n            RawNews(\n                date_utc=_parse_date_utc(row, trading_date),\n                trading_date=trading_date,\n                text=text,\n                text_hash=_text_hash(text),\n                dataset_subset=_parse_subset(extras),\n                source=_parse_source(extras),\n                tickers=_parse_tickers(extras),\n            )\n        )\n\n        if len(batch) &gt;= batch_size:\n            yield batch\n            batch = []\n\n    if batch:\n        yield batch\n\n    logger.info(\"Streaming complete. Skipped %d rows (too short or out of date range).\", skipped)\n</code></pre>"},{"location":"reference/src/Kaare/providers/hf_provider/#src.Kaare.providers.hf_provider.HFNewsProvider.stream_batches(subsets)","title":"<code>subsets</code>","text":"(<code>list[str]</code>)           \u2013            <p>Dataset subset names to load (e.g. <code>[\"sp500_daily_headlines\"]</code>).</p>"},{"location":"reference/src/Kaare/providers/hf_provider/#src.Kaare.providers.hf_provider.HFNewsProvider.stream_batches(start)","title":"<code>start</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive start date filter.</p>"},{"location":"reference/src/Kaare/providers/hf_provider/#src.Kaare.providers.hf_provider.HFNewsProvider.stream_batches(end)","title":"<code>end</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive end date filter.</p>"},{"location":"reference/src/Kaare/providers/hf_provider/#src.Kaare.providers.hf_provider.HFNewsProvider.stream_batches(batch_size)","title":"<code>batch_size</code>","text":"(<code>int</code>, default:                   <code>1000</code> )           \u2013            <p>Number of records per yielded batch.</p>"},{"location":"reference/src/Kaare/providers/yfinance_provider/","title":"yfinance_provider","text":""},{"location":"reference/src/Kaare/providers/yfinance_provider/#src.Kaare.providers.yfinance_provider","title":"src.Kaare.providers.yfinance_provider","text":"<p>yfinance-based fallback data provider (runs in executor to avoid blocking).</p> <p>Classes:</p> <ul> <li> <code>YFinanceProvider</code>           \u2013            <p>Fetches data from Yahoo Finance via the yfinance library.</p> </li> </ul>"},{"location":"reference/src/Kaare/providers/yfinance_provider/#src.Kaare.providers.yfinance_provider.YFinanceProvider","title":"YFinanceProvider","text":"<p>               Bases: <code>DataProvider</code></p> <p>Fetches data from Yahoo Finance via the yfinance library.</p> <p>This provider is used as a fallback when AlphaVantage is unavailable. All blocking I/O is offloaded to a thread pool executor.</p> <p>Methods:</p> <ul> <li> <code>get_gold_prices</code>             \u2013              <p>Fetch daily spot gold prices via yfinance (ticker <code>GC=F</code>).</p> </li> <li> <code>get_stock_ohlcv</code>             \u2013              <p>Fetch OHLCV data from Yahoo Finance.</p> </li> <li> <code>get_treasury_yields</code>             \u2013              <p>Fetch daily 10-year Treasury yields via yfinance (ticker <code>^TNX</code>).</p> </li> <li> <code>get_vix</code>             \u2013              <p>Fetch daily VIX closing values via yfinance (ticker <code>^VIX</code>).</p> </li> </ul>"},{"location":"reference/src/Kaare/providers/yfinance_provider/#src.Kaare.providers.yfinance_provider.YFinanceProvider.get_gold_prices","title":"get_gold_prices  <code>async</code>","text":"<pre><code>get_gold_prices(start: date, end: date) -&gt; dict[datetime.date, float]\n</code></pre> <p>Fetch daily spot gold prices via yfinance (ticker <code>GC=F</code>).</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>dict[date, float]</code>           \u2013            <p>Mapping of date \u2192 gold price (USD).</p> </li> </ul> Source code in <code>src/Kaare/providers/yfinance_provider.py</code> <pre><code>async def get_gold_prices(\n    self,\n    start: datetime.date,\n    end: datetime.date,\n) -&gt; dict[datetime.date, float]:\n    \"\"\"Fetch daily spot gold prices via yfinance (ticker ``GC=F``).\n\n    Args:\n        start: Inclusive start date.\n        end: Inclusive end date.\n\n    Returns:\n        Mapping of date \u2192 gold price (USD).\n    \"\"\"\n    df = await self._download(\"GC=F\", start, end)\n    if df.empty:\n        return {}\n    if isinstance(df.columns, pd.MultiIndex):\n        df.columns = df.columns.get_level_values(0)\n    result: dict[datetime.date, float] = {}\n    for idx, row in df.iterrows():\n        date = idx.date() if hasattr(idx, \"date\") else idx\n        try:\n            result[date] = float(row[\"Close\"])\n        except (KeyError, ValueError):\n            pass\n    return result\n</code></pre>"},{"location":"reference/src/Kaare/providers/yfinance_provider/#src.Kaare.providers.yfinance_provider.YFinanceProvider.get_gold_prices(start)","title":"<code>start</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive start date.</p>"},{"location":"reference/src/Kaare/providers/yfinance_provider/#src.Kaare.providers.yfinance_provider.YFinanceProvider.get_gold_prices(end)","title":"<code>end</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive end date.</p>"},{"location":"reference/src/Kaare/providers/yfinance_provider/#src.Kaare.providers.yfinance_provider.YFinanceProvider.get_stock_ohlcv","title":"get_stock_ohlcv  <code>async</code>","text":"<pre><code>get_stock_ohlcv(symbol: str, start: date, end: date) -&gt; list[StockOHLCV]\n</code></pre> <p>Fetch OHLCV data from Yahoo Finance.</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>list[StockOHLCV]</code>           \u2013            <p>List of :class:<code>~Kaare.models.StockOHLCV</code> records.</p> </li> </ul> Source code in <code>src/Kaare/providers/yfinance_provider.py</code> <pre><code>async def get_stock_ohlcv(\n    self,\n    symbol: str,\n    start: datetime.date,\n    end: datetime.date,\n) -&gt; list[StockOHLCV]:\n    \"\"\"Fetch OHLCV data from Yahoo Finance.\n\n    Args:\n        symbol: Ticker symbol.\n        start: Inclusive start date.\n        end: Inclusive end date.\n\n    Returns:\n        List of :class:`~Kaare.models.StockOHLCV` records.\n    \"\"\"\n    df = await self._download(symbol, start, end)\n    if df.empty:\n        return []\n\n    # Flatten MultiIndex columns if present (yfinance \u22650.2 returns them)\n    if isinstance(df.columns, pd.MultiIndex):\n        df.columns = df.columns.get_level_values(0)\n\n    records: list[StockOHLCV] = []\n    for idx, row in df.iterrows():\n        date = idx.date() if hasattr(idx, \"date\") else idx\n        records.append(\n            StockOHLCV(\n                symbol=symbol,\n                date=date,\n                open=float(row[\"Open\"]),\n                high=float(row[\"High\"]),\n                low=float(row[\"Low\"]),\n                close=float(row[\"Close\"]),\n                volume=int(row[\"Volume\"]),\n            )\n        )\n    return records\n</code></pre>"},{"location":"reference/src/Kaare/providers/yfinance_provider/#src.Kaare.providers.yfinance_provider.YFinanceProvider.get_stock_ohlcv(symbol)","title":"<code>symbol</code>","text":"(<code>str</code>)           \u2013            <p>Ticker symbol.</p>"},{"location":"reference/src/Kaare/providers/yfinance_provider/#src.Kaare.providers.yfinance_provider.YFinanceProvider.get_stock_ohlcv(start)","title":"<code>start</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive start date.</p>"},{"location":"reference/src/Kaare/providers/yfinance_provider/#src.Kaare.providers.yfinance_provider.YFinanceProvider.get_stock_ohlcv(end)","title":"<code>end</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive end date.</p>"},{"location":"reference/src/Kaare/providers/yfinance_provider/#src.Kaare.providers.yfinance_provider.YFinanceProvider.get_treasury_yields","title":"get_treasury_yields  <code>async</code>","text":"<pre><code>get_treasury_yields(start: date, end: date) -&gt; dict[datetime.date, float]\n</code></pre> <p>Fetch daily 10-year Treasury yields via yfinance (ticker <code>^TNX</code>).</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>dict[date, float]</code>           \u2013            <p>Mapping of date \u2192 yield (%).</p> </li> </ul> Source code in <code>src/Kaare/providers/yfinance_provider.py</code> <pre><code>async def get_treasury_yields(\n    self,\n    start: datetime.date,\n    end: datetime.date,\n) -&gt; dict[datetime.date, float]:\n    \"\"\"Fetch daily 10-year Treasury yields via yfinance (ticker ``^TNX``).\n\n    Args:\n        start: Inclusive start date.\n        end: Inclusive end date.\n\n    Returns:\n        Mapping of date \u2192 yield (%).\n    \"\"\"\n    df = await self._download(\"^TNX\", start, end)\n    if df.empty:\n        return {}\n    if isinstance(df.columns, pd.MultiIndex):\n        df.columns = df.columns.get_level_values(0)\n    result: dict[datetime.date, float] = {}\n    for idx, row in df.iterrows():\n        date = idx.date() if hasattr(idx, \"date\") else idx\n        try:\n            result[date] = float(row[\"Close\"])\n        except (KeyError, ValueError):\n            pass\n    return result\n</code></pre>"},{"location":"reference/src/Kaare/providers/yfinance_provider/#src.Kaare.providers.yfinance_provider.YFinanceProvider.get_treasury_yields(start)","title":"<code>start</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive start date.</p>"},{"location":"reference/src/Kaare/providers/yfinance_provider/#src.Kaare.providers.yfinance_provider.YFinanceProvider.get_treasury_yields(end)","title":"<code>end</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive end date.</p>"},{"location":"reference/src/Kaare/providers/yfinance_provider/#src.Kaare.providers.yfinance_provider.YFinanceProvider.get_vix","title":"get_vix  <code>async</code>","text":"<pre><code>get_vix(start: date, end: date) -&gt; dict[datetime.date, float]\n</code></pre> <p>Fetch daily VIX closing values via yfinance (ticker <code>^VIX</code>).</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>dict[date, float]</code>           \u2013            <p>Mapping of date \u2192 VIX closing value.</p> </li> </ul> Source code in <code>src/Kaare/providers/yfinance_provider.py</code> <pre><code>async def get_vix(\n    self,\n    start: datetime.date,\n    end: datetime.date,\n) -&gt; dict[datetime.date, float]:\n    \"\"\"Fetch daily VIX closing values via yfinance (ticker ``^VIX``).\n\n    Args:\n        start: Inclusive start date.\n        end: Inclusive end date.\n\n    Returns:\n        Mapping of date \u2192 VIX closing value.\n    \"\"\"\n    df = await self._download(\"^VIX\", start, end)\n    if df.empty:\n        return {}\n    if isinstance(df.columns, pd.MultiIndex):\n        df.columns = df.columns.get_level_values(0)\n    result: dict[datetime.date, float] = {}\n    for idx, row in df.iterrows():\n        date = idx.date() if hasattr(idx, \"date\") else idx\n        try:\n            result[date] = float(row[\"Close\"])\n        except (KeyError, ValueError):\n            pass\n    return result\n</code></pre>"},{"location":"reference/src/Kaare/providers/yfinance_provider/#src.Kaare.providers.yfinance_provider.YFinanceProvider.get_vix(start)","title":"<code>start</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive start date.</p>"},{"location":"reference/src/Kaare/providers/yfinance_provider/#src.Kaare.providers.yfinance_provider.YFinanceProvider.get_vix(end)","title":"<code>end</code>","text":"(<code>date</code>)           \u2013            <p>Inclusive end date.</p>"},{"location":"reference/src/Kaare/seed/","title":"seed","text":""},{"location":"reference/src/Kaare/seed/#src.Kaare.seed","title":"src.Kaare.seed","text":"<p>Seed script \u2014 populate Kaare with the 10 largest stocks, macro data, and FinBERT sentiment.</p> <p>Usage::</p> <pre><code># Full seed (stocks + macro + sentiment pipeline)\nuv run python -m Kaare.seed\n\n# Sentiment pipeline only (after stocks/macro are already seeded)\nuv run python -m Kaare.seed --pipeline-only\n\n# Quick test with a single small subset\nuv run python -m Kaare.seed --subset sp500_daily_headlines\n</code></pre>"},{"location":"reference/src/Kaare/sentiment/","title":"sentiment","text":""},{"location":"reference/src/Kaare/sentiment/#src.Kaare.sentiment","title":"src.Kaare.sentiment","text":"<p>FinBERT-based sentiment analysis for market news.</p> <p>Classes:</p> <ul> <li> <code>FinBERTAnalyzer</code>           \u2013            <p>Lazy-loading FinBERT sentiment analyzer.</p> </li> </ul>"},{"location":"reference/src/Kaare/sentiment/#src.Kaare.sentiment.FinBERTAnalyzer","title":"FinBERTAnalyzer","text":"<p>Lazy-loading FinBERT sentiment analyzer.</p> <p>The model is loaded from HuggingFace on first use. All inference runs in a thread-pool executor so the async event loop is never blocked.</p> <p>Example::</p> <pre><code>analyzer = FinBERTAnalyzer()\nscore = await analyzer.score([\"Markets rally on strong jobs data.\"])\n# score is a float in [-1.0, 1.0]\n</code></pre> <p>Methods:</p> <ul> <li> <code>score</code>             \u2013              <p>Async wrapper \u2014 scores texts in a thread-pool executor.</p> </li> <li> <code>score_articles</code>             \u2013              <p>Async wrapper \u2014 scores each text individually.</p> </li> <li> <code>score_articles_stream</code>             \u2013              <p>Async generator that yields a score dict for each text as it completes.</p> </li> </ul>"},{"location":"reference/src/Kaare/sentiment/#src.Kaare.sentiment.FinBERTAnalyzer.score","title":"score  <code>async</code>","text":"<pre><code>score(texts: list[str]) -&gt; float\n</code></pre> <p>Async wrapper \u2014 scores texts in a thread-pool executor.</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>float</code>           \u2013            <p>Average FinBERT sentiment score in <code>[-1.0, 1.0]</code>.</p> </li> </ul> Source code in <code>src/Kaare/sentiment.py</code> <pre><code>async def score(self, texts: list[str]) -&gt; float:\n    \"\"\"Async wrapper \u2014 scores *texts* in a thread-pool executor.\n\n    Args:\n        texts: List of news article texts (title + summary).\n\n    Returns:\n        Average FinBERT sentiment score in ``[-1.0, 1.0]``.\n    \"\"\"\n    loop = asyncio.get_running_loop()\n    return await loop.run_in_executor(None, partial(self._score_sync, texts))\n</code></pre>"},{"location":"reference/src/Kaare/sentiment/#src.Kaare.sentiment.FinBERTAnalyzer.score(texts)","title":"<code>texts</code>","text":"(<code>list[str]</code>)           \u2013            <p>List of news article texts (title + summary).</p>"},{"location":"reference/src/Kaare/sentiment/#src.Kaare.sentiment.FinBERTAnalyzer.score_articles","title":"score_articles  <code>async</code>","text":"<pre><code>score_articles(texts: list[str]) -&gt; list[dict]\n</code></pre> <p>Async wrapper \u2014 scores each text individually.</p> <p>Parameters:</p> <p>Returns:</p> <ul> <li> <code>list[dict]</code>           \u2013            <p>List of dicts with <code>positive</code>, <code>negative</code>, <code>neutral</code>,</p> </li> <li> <code>list[dict]</code>           \u2013            <p><code>net_score</code> per article.</p> </li> </ul> Source code in <code>src/Kaare/sentiment.py</code> <pre><code>async def score_articles(self, texts: list[str]) -&gt; list[dict]:\n    \"\"\"Async wrapper \u2014 scores each text individually.\n\n    Args:\n        texts: Article texts to score.\n\n    Returns:\n        List of dicts with ``positive``, ``negative``, ``neutral``,\n        ``net_score`` per article.\n    \"\"\"\n    loop = asyncio.get_running_loop()\n    return await loop.run_in_executor(None, partial(self._score_articles_sync, texts))\n</code></pre>"},{"location":"reference/src/Kaare/sentiment/#src.Kaare.sentiment.FinBERTAnalyzer.score_articles(texts)","title":"<code>texts</code>","text":"(<code>list[str]</code>)           \u2013            <p>Article texts to score.</p>"},{"location":"reference/src/Kaare/sentiment/#src.Kaare.sentiment.FinBERTAnalyzer.score_articles_stream","title":"score_articles_stream  <code>async</code>","text":"<pre><code>score_articles_stream(texts: list[str])\n</code></pre> <p>Async generator that yields a score dict for each text as it completes.</p> <p>Runs one article at a time in the thread-pool executor so the caller can stream results back to the client without waiting for the full batch.</p> <p>Parameters:</p> <p>Yields:</p> <ul> <li>           \u2013            <p>Dict with <code>positive</code>, <code>negative</code>, <code>neutral</code>, <code>net_score</code>.</p> </li> </ul> Source code in <code>src/Kaare/sentiment.py</code> <pre><code>async def score_articles_stream(self, texts: list[str]):\n    \"\"\"Async generator that yields a score dict for each text as it completes.\n\n    Runs one article at a time in the thread-pool executor so the caller can\n    stream results back to the client without waiting for the full batch.\n\n    Args:\n        texts: Article texts to score.\n\n    Yields:\n        Dict with ``positive``, ``negative``, ``neutral``, ``net_score``.\n    \"\"\"\n    loop = asyncio.get_running_loop()\n    for text in texts:\n        yield await loop.run_in_executor(None, partial(self._score_one_sync, text))\n</code></pre>"},{"location":"reference/src/Kaare/sentiment/#src.Kaare.sentiment.FinBERTAnalyzer.score_articles_stream(texts)","title":"<code>texts</code>","text":"(<code>list[str]</code>)           \u2013            <p>Article texts to score.</p>"},{"location":"reference/src/Kaare/smoke_test/","title":"smoke_test","text":""},{"location":"reference/src/Kaare/smoke_test/#src.Kaare.smoke_test","title":"src.Kaare.smoke_test","text":"<p>Quick smoke test for KaareClient against the remote DB.</p>"},{"location":"reference/src/Kaare/smoke_test_sentiment/","title":"smoke_test_sentiment","text":""},{"location":"reference/src/Kaare/smoke_test_sentiment/#src.Kaare.smoke_test_sentiment","title":"src.Kaare.smoke_test_sentiment","text":"<p>Smoke tests for get_stock_news_sentiment routing logic.</p> <p>Tests that: - Historical dates (&gt;1 year ago) are served from the seeded DB - Recent dates (&lt;1 year ago) are fetched from Finnhub and stored in DB - Edge case: date exactly at the 1-year boundary routes to DB - Empty result is handled gracefully (no articles found)</p> <p>Usage::</p> <pre><code>cd src\nuv run python -m Kaare.smoke_test_sentiment\n</code></pre> <p>Functions:</p> <ul> <li> <code>test_boundary_routes_to_db</code>             \u2013              <p>Date range ending exactly at ONE_YEAR_AGO should route to DB.</p> </li> <li> <code>test_empty_result</code>             \u2013              <p>Obscure ticker with no data should return article_count=0 without crashing.</p> </li> <li> <code>test_historical_reads_from_db</code>             \u2013              <p>Data stored via Finnhub should be retrievable from daily_ticker_sentiment.</p> </li> <li> <code>test_recent_reads_from_finnhub</code>             \u2013              <p>Date range &lt;1 year ago should be fetched from Finnhub and stored in DB.</p> </li> </ul>"},{"location":"reference/src/Kaare/smoke_test_sentiment/#src.Kaare.smoke_test_sentiment.test_boundary_routes_to_db","title":"test_boundary_routes_to_db  <code>async</code>","text":"<pre><code>test_boundary_routes_to_db(client: KaareClient) -&gt; bool\n</code></pre> <p>Date range ending exactly at ONE_YEAR_AGO should route to DB.</p> Source code in <code>src/Kaare/smoke_test_sentiment.py</code> <pre><code>async def test_boundary_routes_to_db(client: KaareClient) -&gt; bool:\n    \"\"\"Date range ending exactly at ONE_YEAR_AGO should route to DB.\"\"\"\n    start = ONE_YEAR_AGO - datetime.timedelta(days=7)\n    end = ONE_YEAR_AGO\n    print(f\"\\nBoundary path ({start} \u2192 {end}, end == 1 year ago, expects DB):\")\n\n    sentiment = await client.get_stock_news_sentiment(SYMBOL, start, end)\n\n    ok = sentiment.symbol == SYMBOL and isinstance(sentiment.avg_score, float)\n    result(\"returned valid result\", ok, f\"article_count={sentiment.article_count}\")\n    return ok\n</code></pre>"},{"location":"reference/src/Kaare/smoke_test_sentiment/#src.Kaare.smoke_test_sentiment.test_empty_result","title":"test_empty_result  <code>async</code>","text":"<pre><code>test_empty_result(client: KaareClient) -&gt; bool\n</code></pre> <p>Obscure ticker with no data should return article_count=0 without crashing.</p> Source code in <code>src/Kaare/smoke_test_sentiment.py</code> <pre><code>async def test_empty_result(client: KaareClient) -&gt; bool:\n    \"\"\"Obscure ticker with no data should return article_count=0 without crashing.\"\"\"\n    start = ONE_YEAR_AGO - datetime.timedelta(days=14)\n    end = ONE_YEAR_AGO - datetime.timedelta(days=7)\n    symbol = \"ZZZZ\"\n    print(f\"\\nEmpty result path ({symbol} {start} \u2192 {end}):\")\n\n    sentiment = await client.get_stock_news_sentiment(symbol, start, end)\n\n    ok_count = sentiment.article_count == 0\n    result(\"article_count == 0\", ok_count, str(sentiment.article_count))\n\n    ok_score = sentiment.avg_score == 0.0\n    result(\"avg_score == 0.0\", ok_score, str(sentiment.avg_score))\n\n    ok_daily = sentiment.daily_scores == {}\n    result(\"daily_scores is empty\", ok_daily)\n\n    return ok_count and ok_score and ok_daily\n</code></pre>"},{"location":"reference/src/Kaare/smoke_test_sentiment/#src.Kaare.smoke_test_sentiment.test_historical_reads_from_db","title":"test_historical_reads_from_db  <code>async</code>","text":"<pre><code>test_historical_reads_from_db(client: KaareClient) -&gt; bool\n</code></pre> <p>Data stored via Finnhub should be retrievable from daily_ticker_sentiment.</p> <p>Uses the same recent date range as the recent path test so we know data exists. Queries the DB directly and compares against what get_stock_news_sentiment returns.</p> Source code in <code>src/Kaare/smoke_test_sentiment.py</code> <pre><code>async def test_historical_reads_from_db(client: KaareClient) -&gt; bool:\n    \"\"\"Data stored via Finnhub should be retrievable from daily_ticker_sentiment.\n\n    Uses the same recent date range as the recent path test so we know data exists.\n    Queries the DB directly and compares against what get_stock_news_sentiment returns.\n    \"\"\"\n    end = TODAY\n    start = TODAY - datetime.timedelta(days=7)\n    print(f\"\\nDB match path ({start} \u2192 {end}, result must match daily_ticker_sentiment):\")\n\n    from Kaare.db import repository\n\n    db_rows = await repository.get_daily_ticker_sentiment(client._db, SYMBOL, start, end)\n\n    if not db_rows:\n        result(\"no rows in daily_ticker_sentiment yet (run recent path first)\", True)\n        return True\n\n    sentiment = await client.get_stock_news_sentiment(SYMBOL, start, end)\n\n    db_daily = {date: score for date, score, _ in db_rows}\n    db_article_count = sum(count for _, _, count in db_rows)\n\n    ok_count = sentiment.article_count &gt;= db_article_count\n    result(\"article_count &gt;= DB count\", ok_count, f\"{sentiment.article_count} &gt;= {db_article_count}\")\n\n    ok_dates = set(db_daily.keys()) == set(sentiment.daily_scores.keys())\n    result(\"daily_scores dates match DB\", ok_dates, f\"DB={sorted(db_daily)}\")\n\n    ok_scores = all(\n        abs(sentiment.daily_scores[d] - db_daily[d]) &lt; 1e-6\n        for d in db_daily\n        if d in sentiment.daily_scores\n    )\n    result(\"daily_scores values match DB\", ok_scores)\n\n    return ok_count and ok_dates and ok_scores\n</code></pre>"},{"location":"reference/src/Kaare/smoke_test_sentiment/#src.Kaare.smoke_test_sentiment.test_recent_reads_from_finnhub","title":"test_recent_reads_from_finnhub  <code>async</code>","text":"<pre><code>test_recent_reads_from_finnhub(client: KaareClient) -&gt; bool\n</code></pre> <p>Date range &lt;1 year ago should be fetched from Finnhub and stored in DB.</p> Source code in <code>src/Kaare/smoke_test_sentiment.py</code> <pre><code>async def test_recent_reads_from_finnhub(client: KaareClient) -&gt; bool:\n    \"\"\"Date range &lt;1 year ago should be fetched from Finnhub and stored in DB.\"\"\"\n    end = TODAY\n    start = TODAY - datetime.timedelta(days=7)\n    print(f\"\\nRecent path ({start} \u2192 {end}, expects Finnhub + stored in DB):\")\n\n    sentiment = await client.get_stock_news_sentiment(SYMBOL, start, end)\n\n    ok_symbol = sentiment.symbol == SYMBOL\n    result(\"symbol matches\", ok_symbol, sentiment.symbol)\n\n    ok_type = isinstance(sentiment.avg_score, float)\n    result(\"avg_score is float\", ok_type, str(sentiment.avg_score))\n\n    ok_range = -1.0 &lt;= sentiment.avg_score &lt;= 1.0\n    result(\"avg_score in [-1, 1]\", ok_range, f\"{sentiment.avg_score:.4f}\")\n\n    ok_count = sentiment.article_count &gt;= 0\n    result(\"article_count &gt;= 0\", ok_count, str(sentiment.article_count))\n\n    if sentiment.article_count == 0:\n        result(\"Finnhub returned 0 articles (weekend/holiday or key missing)\", True)\n        return ok_symbol and ok_type and ok_range and ok_count\n\n    result(\"Finnhub returned articles\", True, f\"{sentiment.article_count} articles\")\n    ok_daily = all(-1.0 &lt;= score &lt;= 1.0 for score in sentiment.daily_scores.values())\n    result(\"all daily_scores in [-1, 1]\", ok_daily)\n\n    # Verify articles were actually persisted to daily_ticker_sentiment\n    from Kaare.db import repository\n    db_rows = await repository.get_daily_ticker_sentiment(client._db, SYMBOL, start, end)\n    ok_persisted = len(db_rows) &gt; 0\n    result(\"stored in daily_ticker_sentiment\", ok_persisted, f\"{len(db_rows)} rows in DB\")\n\n    return ok_symbol and ok_type and ok_range and ok_count and ok_daily and ok_persisted\n</code></pre>"},{"location":"reference/src/","title":"src","text":""},{"location":"reference/src/#src","title":"src","text":"<p>Modules:</p> <ul> <li> <code>Kaare</code>           \u2013            <p>Kaare \u2014 async cache-first data layer for stock and macro data.</p> </li> <li> <code>kaare_example</code>           \u2013            <p>Example usage of the KaareClient.</p> </li> </ul>"},{"location":"reference/src/kaare_example/","title":"kaare_example","text":""},{"location":"reference/src/kaare_example/#src.kaare_example","title":"src.kaare_example","text":"<p>Example usage of the KaareClient.</p>"},{"location":"reference/src/run_baseline/","title":"run_baseline","text":""},{"location":"reference/src/run_baseline/#src.run_baseline","title":"src.run_baseline","text":""},{"location":"reference/src/stock_predictor/","title":"stock_predictor","text":""},{"location":"reference/src/stock_predictor/#src.stock_predictor","title":"src.stock_predictor","text":""},{"location":"reference/src/yfinanceLoader/","title":"yfinanceLoader","text":""},{"location":"reference/src/yfinanceLoader/#src.yfinanceLoader","title":"src.yfinanceLoader","text":""}]}